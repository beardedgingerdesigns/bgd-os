# Retro — 2026-06-19

**Period:** 2026-06-12 through 2026-06-19 (7 days)
**Sources:** Git log (32 commits, claude-os only), 4 Cowork session transcripts (automated triage), 3 session wraps, decisions log, todos, brainstorm docs.
**Limitation:** `~/.claude/projects` JSONL files are not accessible from the Cowork sandbox, so this retro covers only claude-os repo activity + Cowork triage sessions. Other repos (brandos, thermal-kitchen, tonequest, global-ag-network) are visible only through state files and session wraps routed into claude-os. **Narrower coverage than last week's 11-repo / 568-message retro.**

## Security flags

None. No pasted secrets/tokens found in session transcripts or wraps.

## Week shape

- **claude-os (dominant):** 32 commits. Two massive workstreams: AIOS UI v2 operator console (three-panel layout, chat, 4 context views, 31 files / 2763 additions) and triage hardening (state-writeback detector, show-what-they-said, recency anchor fix, pattern mute-list). Also: Cowork scheduled task setup (7am + 1pm triage), dispatch backlog clear (9 items routed), retro, audit (90/100 — first Stage 3: Autonomous), GAN kickoff, reference guides for MCP APIs.
- **Cowork triage automation:** 4 scheduled runs visible. 3 succeeded, 1 hit a 500 API error and produced zero output. Afternoon 6/18 found 3 reply threads + billing issues. Morning 6/19 found clean inbox. Afternoon 6/19 found 3 reply threads + 3 billing actions.
- **Cross-project (via state files / wraps):** Wild Rose go-live moved July 1 → July 13 (Aaron prefers no holiday-week launch). Thermal Kitchen Phase 1 complete, standing call being rescheduled. ToneQuest check-in happened. GAN touchbase happened (podcast inspo, old Gmail account cleanup). Servd card declined — Wild Rose hosting goes offline June 23 if not fixed.

---

## Ranked patterns

### 1. Triage automation is the recurring engineering sink

- **Type:** INVESTMENT PATTERN
- **Count:** 5+ distinct triage improvements this week
- **Weekly cost:** HIGH (largest engineering investment after AIOS UI)
- **Evidence:**
  - 6/19: Set up Cowork scheduled tasks (7am + 1pm), updated skill to write `state/inbox-triage.md`
  - 6/19: Built state-writeback detector (5 units: envelope parser, reconcile-on-read, strip envelope, types) — because Sync queue was empty when it shouldn't have been
  - 6/19: Triage hardening brainstorm + requirements doc + feature commit — because afternoon run misclassified Inside Out (Melinda replied that morning but was filed as "21d nudge candidate")
  - 6/19: New `state/triage-mutes.md` — because personal email (Camp Mitigwa), calendar accepts, Gemini notes leaked into Reply Today
  - 6/19: Code review caught apply-resurrection P1 bug in the detector
- **Assessment:** The investment is justified — triage is the one automation Justin uses every day and quality gaps directly waste his time. But the volume of correctness issues (recency anchoring, content gaps, noise filtering, detector not firing) suggests the skill's prompt needs an integration test suite, not just per-incident fixes. Each fix has been reactive to a specific misclassification.
- **Proposed fix:** Build a triage eval set — 10-15 representative inbox snapshots with known-correct classifications. Run after each skill edit. Would catch regression and shift from reactive to proactive.

### 2. Scheduled task reliability — 25% failure rate

- **Type:** FRICTION
- **Count:** 1 failure out of 4 visible runs
- **Weekly cost:** MEDIUM
- **Evidence:**
  - Cowork session `local_040d0df7` (morning triage): "API Error: 500 Internal server error" — entire run produced nothing
  - The other 3 runs completed successfully with full triage output
- **Assessment:** A scheduled task that fails silently (no retry, no alert) means Justin's morning briefing just doesn't exist that day. He may not even notice it didn't run.
- **Proposed fix:** Add retry logic to the scheduled task (re-invoke on 500 after 60s delay). And/or a canary: if `state/inbox-triage.md` wasn't updated in the expected window, surface it in the next run's output.

### 3. Subagent idle loop (continuing — partially mitigated)

- **Type:** FRICTION (recurring from retro-2026-06-18 pattern #1)
- **Count:** 9 idle agents in one session (6/19 wrap), plus successful agent usage in a later session
- **Weekly cost:** MEDIUM (down from HIGH)
- **Evidence:**
  - 6/19 early session: 9 agents went idle — triage researchers (2), retro analyzers (3), UI view builders (4). All abandoned and work done inline.
  - CLAUDE.md rule written during that session: "if a subagent sends 2 idle notifications without results, abandon and run inline"
  - 6/19 later session: `ce-plan`, `ce-work`, `ce-code-review` agents ran successfully for triage hardening feature (5 units + review)
- **Assessment:** The CLAUDE.md rule is helping — later sessions used agents more selectively and they worked. The pattern isn't gone but the cost is going down. The rule is doing its job as a workaround; the root cause (Cowork/Claude Code agent idle bug) is platform-level.
- **Status:** Mitigated. Keep monitoring. No new action needed.

### 4. Hidden broken state across sessions

- **Type:** FRICTION (variant of retro-2026-06-18 pattern #2)
- **Count:** 2 instances
- **Weekly cost:** MEDIUM
- **Evidence:**
  - 6/19 triage hardening session: "committed tree didn't build on fresh checkout — untracked pending-todos module. A pre-existing loose end the handoff didn't mention; caught only by inspecting untracked files."
  - 6/19 triage hardening session: "Dirty working tree noise: many unrelated uncommitted files (state/*, context docs) forced scoping the review diff"
- **Assessment:** The branch had code that imported an untracked file — it compiled locally (file existed in working tree) but would fail on fresh checkout. This is the same pattern as last week's "context loss" but manifesting as code-level rather than document-level. /wrap doesn't catch build-health issues.
- **Proposed fix:** Add a `tsc --noEmit` or `npm run build` check to /wrap before committing. If the build breaks, flag it before the session ends.

### 5. Payment card expiration cascade

- **Type:** OPERATIONAL
- **Count:** 3 services in one week
- **Weekly cost:** LOW (but the Servd one is deadline-critical)
- **Evidence:**
  - Servd hosting for Wild Rose — card declined, **site goes offline June 23** (4 days from triage)
  - DeployHQ — card declined, decided to let it lapse
  - ChatGPT Plus — payment method needs updating (3 emails over 3 days)
- **Assessment:** Three payment failures in one week suggests a card expiration or replacement event. Not an AIOS problem per se, but the Servd one is business-critical (Wild Rose is a $1,000/mo client launching July 13).
- **Proposed fix:** Not automatable. Just a flag: update the payment method on Servd immediately. The triage is already surfacing these correctly; the gap is action, not detection.

### 6. Retro coverage gap — Cowork can't see Claude Code sessions

- **Type:** TOOLING GAP
- **Count:** 1 (this retro)
- **Weekly cost:** LOW (but compounds)
- **Evidence:**
  - `retro-extract.py` found 0 messages — `~/.claude/projects` doesn't exist in the Cowork sandbox
  - This retro sees only claude-os + 4 Cowork triage sessions. Last week's retro covered 11 repos / 568 messages from Claude Code terminal sessions.
  - All interactive project work (brandos, thermal-kitchen, tonequest, GAN) is invisible to a Cowork-scheduled retro
- **Assessment:** If the retro is going to run as a Cowork scheduled task, it needs a data bridge to Claude Code session history. Without it, retros will only see AIOS-layer activity and miss all project-level patterns.
- **Proposed fix:** Pre-extract step: run `retro-extract.py` from a Claude Code session (or a cron job) and write output to `archives/raw/retro-extract-latest/`. The Cowork retro reads those files. Alternatively, mount `~/.claude/projects` into the Cowork session (may not be possible).

---

## #1 candidate for next /level-up

**Triage eval set.** The triage skill is the most-used automation and the most-fixed one. This week alone: recency anchor bug, missing content, noise leak, detector not firing, apply-resurrection bug. Each was caught by Justin or by manual testing after a live run went wrong. A deterministic eval set (10-15 inbox snapshots → expected output) would catch regressions before they reach Justin's inbox. This is a "vending machine" automation (deterministic input → expected output), not a "slot machine" — perfect for an eval harness.

## Cross-project signals

- **Triage is the one automation that runs every day.** It deserves the most rigorous testing. Everything else (retro, dispatch, brief, audit) runs weekly or ad-hoc.
- **AIOS UI v2 shipped and is now the context surface.** The operator console replaces the terminal for AIOS work. Quality of the data it renders (triage, state, todos) now directly affects Justin's daily workflow.
- **Wild Rose Servd billing is the highest-urgency item in the system right now** — not a pattern, just a flag. June 23 deadline, July 13 launch, $1,000/mo client.

## Comparison to last week

| Pattern | Last week | This week | Trend |
|---|---|---|---|
| Subagent idle loop | #1, HIGH | #3, MEDIUM | Mitigated (CLAUDE.md rule) |
| Context loss / handoff tax | #2, HIGH | #4, MEDIUM (variant: broken build state) | Partially addressed |
| "Just do it" / autonomy friction | #3, MEDIUM | Not observed | May be resolved or just not visible (limited sources) |
| Wrap/wiki verification nag | #4, MEDIUM | Not observed | Same caveat |
| Screenshot relay | #5, MEDIUM | Not observed | Same caveat |
| Manual git directives | #6, LOW | Not observed | Same caveat |
| Triage quality issues | Not ranked | #1, HIGH | New — emerged as triage scaled to daily automated runs |
| Scheduled task reliability | Not ranked | #2, MEDIUM | New — first week of Cowork scheduled tasks |

**Note:** Patterns 3-6 from last week may still be active in project repos (brandos, thermal-kitchen) but are invisible to this retro due to the Cowork sandbox limitation (pattern #6 above).
