# Requirements — synthesized intel

No PRD-type docs were classified in this ingest. PRDs are absent.

The v3 SPEC and v3 plan are partially PRD-like (they list operator features with acceptance) but were classified `SPEC` and `DOC` respectively per the manifest override — so their feature content lives in `constraints.md` (technical) and `context.md` (operator intent), not here.

If/when PRDs are added in a future ingest, they land in this file.

---

## (empty)
