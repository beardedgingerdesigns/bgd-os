# Requirements: v1.0 — AIOS UI v0→v2 (SHIPPED 2026-05-22)

**Status:** ✅ All 26 v1 requirements validated.

> **Provenance.** No PRDs were ingested. Requirements were DERIVED from operator-intent context (ADR Context/Consequences + v3 SPEC operator-pain narrative + LOCKED ADR decisions). Treated as soft acceptance criteria; LOCKED ADRs win in any conflict.

---

## Read-only Navigation & MRR (NAV) — VALIDATED, Phase 1

- [x] **NAV-01**: Home page renders Client cards + Admin card + MRR widget + daily-ingest button + Recent activity + "Onboard new client" button, no sidebar.
- [x] **NAV-02**: Client page at `/clients/{slug}` shows project sidebar, contacts, client-level MRR rollup, project cards, scoped activity.
- [x] **NAV-03**: Project page at `/clients/{slug}/projects/{project-slug}` shows project sidebar, header + compact contacts + activity, persistent breadcrumb.
- [x] **NAV-04**: MRR widget sums `mrr_monthly` for `bucket == "paying"` AND `status == "active"`.
- [x] **NAV-05**: Client page MRR rollup uses same filter rule applied to that Client's Projects.

## Live Filesystem Sync & Daily Triage (SYNC) — VALIDATED, Phase 2

- [x] **SYNC-01**: `chokidar` watcher + SSE invalidation propagates `clients.yaml` / `references/*.md` / `decisions/log.md` / `memory/project_*.md` edits to UI without manual reload (250ms debounce).
- [x] **SYNC-02**: "Run daily ingest" on Home spawns `/daily-inbox-triage` and streams ranked output.
- [x] **SYNC-03**: Same-day re-clicks show cached output with "Run again" button.
- [x] **SYNC-04**: Each triage row is a Gmail deep-link (`mail.google.com/mail/u/0/#inbox/<thread-id>`).

## Operator Capture & Ritual Launchers (OPS) — VALIDATED, Phase 3

- [x] **OPS-01**: Project pages render Capture box with ⌘↵ submit, Escape clear, `aria-live` status.
- [x] **OPS-02**: Capture POSTs to `/api/capture/[client]/[project]`, spawns `claude --print /capture <text>` (90s timeout), streams via SSE.
- [x] **OPS-03**: Admin ritual grid (`level-up`, `weekly-status`, `audit`) shows "Last ran Xd ago" + "run →" affordance; `business-plans` placeholder non-clickable.
- [x] **OPS-04**: `RitualModal` POSTs `/api/admin/[ritual]`, spawns skill (5min timeout), streams live, writes `RitualCacheEntry` on success.
- [x] **OPS-05**: On failure/timeout: streamed output to point of failure, error footer, no cache write, offers Re-run.

## MRR Data Model (DATA) — VALIDATED, Phase 1 (data layer)

- [x] **DATA-01**: `mrr_monthly:` structured field on every Project with recurring revenue.
- [x] **DATA-02**: Legacy free-form `contract:` field preserved for context, never parsed for revenue math.
- [x] **DATA-03**: BrandOS dealer subscriptions are first-class Project entries (not nested under terraplex-hub).
- [x] **DATA-04**: Flipping `bucket: prospects → paying` is the single action for Projects to roll into MRR.

## Bidirectional Hub & Chat (HUB) — VALIDATED, Phase 4

- [x] **HUB-01**: Project page collapsed chat drawer; expanding shows "Brief loaded (built Nm ago)" + "Refresh context" button; hydrates from `.aios-cache/briefs/<slug>.md`. (Delivered by Plan 04-07.)
- [x] **HUB-02**: Brief indexer (`lib/indexer/build-brief.ts`) invokes `/load-project` and captures stdout. Originally paired with chokidar watcher; **watcher removed during gap-closure** (GAP-04-02) — lazy-build on chat open + Refresh button now satisfies the freshness contract.
- [x] **HUB-03**: Chat backend spawns long-running `claude` subprocess per Project, manages session via `claude --resume <session-id>`, persists session IDs in `.aios-cache/sessions.json`, streams via SSE. Explicit contract test in Plan 04-07 Task 4.
- [x] **HUB-04**: Live Gmail thread triage rows + calendar events fetched at chat bootstrap (not embedded in static brief). Per-project triage rows in Communications section deliver the Project-page projection.
- [x] **HUB-05**: Captures, chat-decisions, chat-sessions write to `{wikiPath}/raw/aios/<kind>-YYYY-MM-DD-<slug>.md` (kinds: `capture` / `chat-decision` / `chat-session`); files immutable; AIOS never writes to curated wiki paths.
- [x] **HUB-06**: Every write emits a Receipt feed entry (timestamp, kind, project_slug, absolute file_written, excerpt). Global dock + per-project slice.
- [x] **HUB-07**: Pending Ingestion section lists `raw/aios/` drops awaiting promotion; one-click "Run wiki ingest" invokes `/ingest-aios-drops` skill which drives `llm-wiki` curator flow.
- [x] **HUB-08**: Wiki content (resolved via `wiki_path:` + `docs_paths:`) renders as expandable Project-page sections (Active Decisions / Recent log / Deferred Decisions) with `vscode://file/{path}` click-to-open.
- [x] **HUB-09**: Daily-inbox-triage skill consults `aios-ui/.aios-cache/triage-overrides.json` before Step 2 filter; skips `replied | not_me | dismissed`; skips `snoozed` while `snooze_until > now`. Per-row Replied/Snooze/Not-me actions in Communications section write to that file via `POST /api/triage/override/[threadId]`. Skill also checks Gmail thread participants (not just last sender) for Justin's address.

---

## v2 Requirements deferred to milestone v1.1+

Per v3 SPEC non-goals, the following were intentionally scoped out:

### Capture Surface Expansion (CAPX)

- CAPX-01: Global capture surface accessible from Home
- CAPX-02: Client-level capture surface on Client pages
- CAPX-03: Cmd+K capture palette (keyboard-driven, page-agnostic)
- CAPX-04: Capture preview-then-confirm step (multi-turn review before write)

### Decision Promotion Heuristics (DPH)

- DPH-01: Auto-detect "decision in chat" and offer one-click promote (deferred — current model is explicit operator action via Drop-to-raw button)

### Receipt Feed Polish (RFP)

- RFP-01: Retention policy / pagination beyond last 20
- RFP-02: Filter by kind / project / date range
- RFP-03: Mark-all-seen UX (currently auto-marks on collapse)

### Code Polish (PLSH)

- PLSH-01: Dedupe `extractTextDelta` helper across 5 subprocess wrappers
- PLSH-02: Dedupe `relativeTime` helper across 3 components
- PLSH-03: `receipts.jsonl` rotation policy
- PLSH-04: Replace `Math.random()` receipt IDs with collision-resistant nanoid
- PLSH-05: Slugify Unicode (currently non-Latin → 'untitled')
- PLSH-06: ProjectReceiptsSlice older-receipts surfacing beyond 100-cap

(IN-01 through IN-06 from `04-REVIEW.md` map to PLSH-01..06.)

---

## Coverage

- v1 requirements: 26 total
- Validated: 26 ✓
- Deferred: 13 (CAPX 4, DPH 1, RFP 3, PLSH 6 — wait for next milestone planning)
- Failed / dropped: 0

_Archived 2026-05-22 from .planning/REQUIREMENTS.md at milestone v1.0 close._
