---
phase: 08-scheduled-triage-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aios-ui/lib/types.ts
  - aios-ui/lib/data/wiki.ts
  - aios-ui/tests/lib/raw-drops.test.ts
autonomous: true
requirements:
  - TRIAGE-04
  - TRIAGE-05
must_haves:
  truths:
    - "RawDropKind type includes 'triage-dispatch' as a valid member"
    - "detectKind recognizes triage-dispatch- prefix and returns a named kind"
    - "PendingFile.kind union includes 'triage-dispatch' alongside 'capture', 'chat-decision', 'chat-session', 'other'"
    - "buildRawDropPath produces correct filename for triage-dispatch kind"
    - "Existing tests remain green after type extension"
  artifacts:
    - path: "aios-ui/lib/types.ts"
      provides: "RawDropKind with triage-dispatch member, PendingFile.kind with triage-dispatch"
      contains: "triage-dispatch"
    - path: "aios-ui/lib/data/wiki.ts"
      provides: "detectKind recognizing triage-dispatch- prefix"
      contains: "triage-dispatch"
    - path: "aios-ui/tests/lib/raw-drops.test.ts"
      provides: "Test case for triage-dispatch kind in buildRawDropPath"
      contains: "triage-dispatch"
  key_links:
    - from: "aios-ui/lib/data/wiki.ts"
      to: "aios-ui/lib/types.ts"
      via: "PendingFile.kind type uses the extended union"
      pattern: "triage-dispatch"
    - from: "aios-ui/lib/raw-drops.ts"
      to: "aios-ui/lib/types.ts"
      via: "writeRawDrop accepts RawDropKind which now includes triage-dispatch"
      pattern: "RawDropKind"
---

<objective>
Extend the RawDropKind type and PendingFile.kind union to include 'triage-dispatch', update detectKind in wiki.ts to recognize the new prefix, and add a test case verifying buildRawDropPath works with the new kind. This is the data model foundation that the scheduled triage skill (Plan 02) writes against per D-08 and D-10 (one dispatch file per thread per matched wiki).

Purpose: The scheduled triage routine dispatches project-relevant email intelligence to wiki raw/aios/ directories. The type system and kind detection must recognize triage-dispatch files before the skill can create them.

Output: Updated type definitions, kind detection, and a passing test for the new kind.
</objective>

<execution_context>
@/Users/justinlobaito/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinlobaito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/justinlobaito/repos/claude-os/.planning/PROJECT.md
@/Users/justinlobaito/repos/claude-os/.planning/ROADMAP.md
@/Users/justinlobaito/repos/claude-os/.planning/STATE.md
@/Users/justinlobaito/repos/claude-os/.planning/phases/08-scheduled-triage-automation/08-RESEARCH.md

# Codebase context
@/Users/justinlobaito/repos/claude-os/aios-ui/lib/types.ts
@/Users/justinlobaito/repos/claude-os/aios-ui/lib/data/wiki.ts
@/Users/justinlobaito/repos/claude-os/aios-ui/lib/raw-drops.ts
@/Users/justinlobaito/repos/claude-os/aios-ui/tests/lib/raw-drops.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend RawDropKind type and PendingFile.kind union</name>
  <files>aios-ui/lib/types.ts, aios-ui/lib/data/wiki.ts</files>
  <read_first>
    aios-ui/lib/types.ts
    aios-ui/lib/data/wiki.ts
    aios-ui/lib/raw-drops.ts
  </read_first>
  <action>
    In aios-ui/lib/types.ts at line 242, extend the RawDropKind union to add 'triage-dispatch' as the fourth member:
    export type RawDropKind = 'capture' | 'chat-decision' | 'chat-session' | 'triage-dispatch'

    In aios-ui/lib/types.ts at the PendingFile interface (line 81), extend the kind field to include 'triage-dispatch':
    kind: 'capture' | 'chat-decision' | 'chat-session' | 'triage-dispatch' | 'other'

    In aios-ui/lib/data/wiki.ts, update the detectKind function (line 266) to recognize the triage-dispatch- filename prefix. Add a new condition before the return 'other' fallback:
    if (filename.startsWith('triage-dispatch-')) return 'triage-dispatch'

    The new condition goes after the existing chat-session check (line 269) and before the return 'other' on line 270. This ensures files named triage-dispatch-YYYY-MM-DD-slug.md display with the correct kind in the Pending Ingestion UI instead of falling through to 'other'.
  </action>
  <verify>
    <automated>cd /Users/justinlobaito/repos/claude-os/aios-ui && npx vitest run tests/lib/raw-drops.test.ts tests/lib/content-classifier.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "'triage-dispatch'" aios-ui/lib/types.ts returns 2 (one in RawDropKind, one in PendingFile.kind)
    - grep -c "triage-dispatch" aios-ui/lib/data/wiki.ts returns at least 1 (detectKind branch)
    - Existing tests pass with no regressions
    - cd aios-ui && npx tsc --noEmit 2>&1 | grep -v "\.next/types" exits clean (no new type errors introduced)
  </acceptance_criteria>
  <done>RawDropKind includes 'triage-dispatch'. PendingFile.kind includes 'triage-dispatch'. detectKind maps 'triage-dispatch-' prefix to the named kind. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add triage-dispatch test case to raw-drops tests</name>
  <files>aios-ui/tests/lib/raw-drops.test.ts</files>
  <read_first>
    aios-ui/tests/lib/raw-drops.test.ts
    aios-ui/lib/raw-drops.ts
    aios-ui/lib/types.ts
  </read_first>
  <action>
    In aios-ui/tests/lib/raw-drops.test.ts, add a test case inside the existing buildRawDropPath describe block (after the existing kind test cases around lines 44-67). The new test verifies that buildRawDropPath produces the correct filename pattern for the triage-dispatch kind.

    Test name: "generates correct filename for triage-dispatch kind"
    Input: wikiPath '/tmp/w', kind 'triage-dispatch', slug 'pyro-trade-show-materials'
    Expected: filename contains 'triage-dispatch-' prefix, includes date in YYYY-MM-DD format, and ends with '-pyro-trade-show-materials.md'

    Also add a test case in the writeRawDrop describe block verifying that a triage-dispatch file is written to the correct raw/aios/ directory within the wiki path. Follow the same tmpDir fixture pattern used by the existing writeRawDrop tests (beforeEach creates temp dir, afterEach removes it).

    Test name: "writes triage-dispatch file to raw/aios/ directory"
    Input: wikiPath pointing to tmpDir, kind 'triage-dispatch', slug 'dakota-fest-materials', body with YAML frontmatter (kind: triage-dispatch, thread_id: test123, sender: test@example.com, date: 2026-06-08, subject: Test Subject) and summary body text.
    Assert: file exists at expected path, content includes 'kind: triage-dispatch' and 'thread_id: test123'.
  </action>
  <verify>
    <automated>cd /Users/justinlobaito/repos/claude-os/aios-ui && npx vitest run tests/lib/raw-drops.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "triage-dispatch" aios-ui/tests/lib/raw-drops.test.ts returns at least 4 (kind references in test assertions)
    - All tests in raw-drops.test.ts pass including the two new test cases
    - cd aios-ui && npx vitest run --reporter=verbose 2>&1 | grep -c "triage-dispatch" returns at least 2 (test names in output)
  </acceptance_criteria>
  <done>Two new test cases verify triage-dispatch works correctly with buildRawDropPath and writeRawDrop. The triage-dispatch kind produces files following the ADR 0004 naming convention (triage-dispatch-YYYY-MM-DD-slug.md). All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Email subject -> slug -> filename | Untrusted email subject strings become filenames via slugify() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | triage-dispatch filename | mitigate | slugify() in raw-drops.ts already sanitizes to [a-z0-9-] and assertInside guards path traversal. No additional mitigation needed for the type extension. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
1. `cd /Users/justinlobaito/repos/claude-os/aios-ui && npx vitest run tests/lib/raw-drops.test.ts -x` -- triage-dispatch tests pass
2. `cd /Users/justinlobaito/repos/claude-os/aios-ui && npx vitest run -x` -- full suite green
3. `grep "'triage-dispatch'" aios-ui/lib/types.ts` -- returns 2 matches (RawDropKind + PendingFile.kind)
4. `grep "triage-dispatch" aios-ui/lib/data/wiki.ts` -- returns at least 1 match (detectKind)
</verification>

<success_criteria>
- RawDropKind type includes 'triage-dispatch' as a valid member
- PendingFile.kind union includes 'triage-dispatch' as a valid member
- detectKind in wiki.ts recognizes 'triage-dispatch-' prefix
- Two new test cases pass for triage-dispatch in raw-drops.test.ts
- Full test suite passes with no regressions
- Zero new npm dependencies
</success_criteria>

<output>
Create `.planning/phases/08-scheduled-triage-automation/08-01-SUMMARY.md` when done
</output>
