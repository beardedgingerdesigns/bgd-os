---
phase: 08-scheduled-triage-automation
plan: 03
type: execute
wave: 2
depends_on:
  - 08-02
files_modified: []
autonomous: false
requirements:
  - TRIAGE-01
must_haves:
  truths:
    - "A Desktop scheduled task exists that runs the scheduled-triage skill on a waking-hours cron schedule"
    - "The cron expression fires every 2 hours from 7am-7pm CT on weekdays only (D-02)"
    - "The task uses the scheduled-triage SKILL.md as its prompt"
    - "A manual test run produces observable output (cache file updated, or push notification received)"
  artifacts: []
  key_links:
    - from: "Desktop scheduled task"
      to: ".claude/skills/scheduled-triage/SKILL.md"
      via: "Task references the skill as its execution prompt"
      pattern: "scheduled-triage"
---

<objective>
Create the Claude Code Desktop scheduled task that triggers the scheduled-triage skill on a waking-hours cron schedule (D-01, D-02, TRIAGE-01). This is the activation step -- the skill definition (Plan 02) exists but does not fire until a Desktop task is created to run it. The task setup is done interactively because Desktop scheduled tasks are created through the Claude Code Desktop app UI or by asking Claude in a Desktop session, not through code.

Purpose: This is the final step that makes triage ambient. Without the schedule, the skill exists but never fires automatically. With it, Justin gets triage output every 2 hours during work hours without lifting a finger.

Output: A running Desktop scheduled task with waking-hours cron and a verified first execution.
</objective>

<execution_context>
@/Users/justinlobaito/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinlobaito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/justinlobaito/repos/claude-os/.planning/PROJECT.md
@/Users/justinlobaito/repos/claude-os/.planning/ROADMAP.md
@/Users/justinlobaito/repos/claude-os/.planning/STATE.md
@/Users/justinlobaito/repos/claude-os/.planning/phases/08-scheduled-triage-automation/08-RESEARCH.md
@/Users/justinlobaito/repos/claude-os/.planning/phases/08-scheduled-triage-automation/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Desktop scheduled task for waking-hours triage</name>
  <files></files>
  <read_first>
    .claude/skills/scheduled-triage/SKILL.md
    .planning/phases/08-scheduled-triage-automation/08-RESEARCH.md
  </read_first>
  <action>
    Create a Claude Code Desktop scheduled task that runs the scheduled-triage skill. The task is created by asking Claude (in this Desktop session) to schedule it, or by using the CronCreate tool if available.

    Task configuration:
    - Name: "Scheduled Inbox Triage"
    - Cron expression: 0 */2 7-19 * 1-5
      This fires at 7, 9, 11, 13, 15, 17, 19 (every 2 hours from 7am-7pm, weekdays only).
      Desktop tasks use local timezone (CT) per RESEARCH.md.
    - Type: Local (Desktop task, NOT cloud Routine -- per RESEARCH.md architecture decision)
    - Working directory: /Users/justinlobaito/repos/claude-os
    - Prompt: Run the /scheduled-triage skill. Follow the complete SKILL.md at .claude/skills/scheduled-triage/SKILL.md. Execute all steps 0-9 in order.

    If CronCreate tool is not directly available, output the exact instructions for Justin to create the task via the Desktop app Routines page:
    1. Open Claude Code Desktop
    2. Click "Routines" in sidebar (or navigate to Settings > Routines)
    3. Click "New routine" or "+" button
    4. Select "Local" (not Cloud)
    5. Set name: "Scheduled Inbox Triage"
    6. Set schedule: Custom cron expression: 0 */2 7-19 * 1-5
    7. Set working directory: /Users/justinlobaito/repos/claude-os
    8. Set prompt: "Run the /scheduled-triage skill. Follow .claude/skills/scheduled-triage/SKILL.md. Execute all steps 0-9."
    9. Save and enable the task

    After creation, trigger a manual "Run now" to verify the task executes. Check for:
    - triage-latest.json updated with current timestamp
    - Push notification received (or visible in Desktop app notification log)

    IMPORTANT tradeoff note per D-01: The CONTEXT.md decision says "fires even when laptop is closed" but RESEARCH.md confirmed Desktop tasks require the app to be open and machine awake. This is the correct choice because todos/pending.md, wiki raw/aios/ directories, and triage-overrides.json all require local filesystem access. Surface this tradeoff clearly in the summary. A future phase could migrate to cloud Routines by committing these files to git.

    Fallback if 0 */2 7-19 * 1-5 is rejected by the scheduler: Create 7 separate tasks, one per firing time:
    0 7 * * 1-5, 0 9 * * 1-5, 0 11 * * 1-5, 0 13 * * 1-5, 0 15 * * 1-5, 0 17 * * 1-5, 0 19 * * 1-5
    Each with the same prompt and working directory.
  </action>
  <verify>
    <automated>cat /Users/justinlobaito/repos/claude-os/aios-ui/.aios-cache/triage-latest.json 2>/dev/null | grep -c "ranAt"</automated>
  </verify>
  <acceptance_criteria>
    - A Desktop scheduled task named "Scheduled Inbox Triage" exists (visible in Desktop app Routines page)
    - The task uses cron expression 0 */2 7-19 * 1-5 (or 7 equivalent individual crons as fallback)
    - The task type is Local (not Cloud)
    - The task working directory is /Users/justinlobaito/repos/claude-os
    - A "Run now" test execution completes without errors
    - triage-latest.json exists and contains a ranAt field with a recent timestamp (within last 10 minutes)
  </acceptance_criteria>
  <done>Desktop scheduled task created and verified with a manual test run. The scheduled triage routine will now fire automatically every 2 hours during weekday waking hours (7am-7pm CT).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end scheduled triage pipeline</name>
  <what-built>
    The complete scheduled triage automation pipeline:

    1. Desktop scheduled task "Scheduled Inbox Triage" running on cron 0 */2 7-19 * 1-5 (weekdays 7am-7pm CT)
    2. Scheduled-triage SKILL.md with Steps 0-9 (lookback, filter, score, context, output, drafts, action items, dispatch, cache+notification)
    3. RawDropKind + detectKind extended to recognize triage-dispatch files
    4. Test cases for the new triage-dispatch kind

    Tradeoff surfaced: D-01 says "fires even when laptop is closed" but filesystem dependencies require local access. Desktop tasks are correct for now; cloud Routine migration is a future consideration if missed runs become a problem.
  </what-built>
  <how-to-verify>
    1. Open Claude Code Desktop app. Navigate to Routines/Scheduled Tasks page.
    2. Verify "Scheduled Inbox Triage" task exists with the correct cron schedule.
    3. Click "Run now" (or wait for the next scheduled firing).
    4. After the run completes, check:
       a. Gmail Drafts folder -- do draft replies appear for threads that need responses?
       b. Run: cat /Users/justinlobaito/repos/claude-os/todos/pending.md -- do new triage-sourced items appear in the Pending section?
       c. Run: ls /Users/justinlobaito/repos/claude-os/../*/docs/wiki/*/raw/aios/triage-dispatch-* 2>/dev/null -- do dispatch files appear in any project wiki?
       d. Run: cat /Users/justinlobaito/repos/claude-os/aios-ui/.aios-cache/triage-latest.json -- does ranAt show a recent timestamp?
       e. Did a push notification arrive on your device with the triage summary?
    5. Verify the draft replies in Gmail match your voice register (casual, short sentences, no em dashes).
    6. Verify no emails were SENT (only drafted).
  </how-to-verify>
  <resume-signal>Type "approved" if the scheduled triage is working correctly, or describe any issues with drafts, action items, dispatch files, or the schedule itself.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron schedule -> skill execution | Desktop task scheduler triggers skill with full local filesystem access |
| Skill execution -> Gmail API | Skill creates drafts via MCP -- drafts only, never sends |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Elevation of Privilege | Desktop task filesystem access | accept | Desktop tasks have the same filesystem permissions as the user who created them. Single-operator local system -- no privilege boundary exists. |
| T-08-08 | Spoofing | Scheduled task identity | accept | Desktop tasks run under the operator's Claude Code session. No external identity involved. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
1. Desktop scheduled task visible in Claude Code Desktop Routines page
2. triage-latest.json updated with recent ranAt after test run
3. Push notification received (or visible in notification log)
4. Human verification of Gmail drafts, action items, and dispatch files
</verification>

<success_criteria>
- Desktop scheduled task exists with waking-hours cron (0 */2 7-19 * 1-5 or 7 individual crons)
- Manual test run completes successfully
- Operator verifies drafts appear in Gmail, action items in todos/pending.md, dispatch files in wiki raw/aios/
- Operator confirms no emails were sent (draft-only)
- Phase 8 is complete: triage is now ambient infrastructure
</success_criteria>

<output>
Create `.planning/phases/08-scheduled-triage-automation/08-03-SUMMARY.md` when done
</output>
