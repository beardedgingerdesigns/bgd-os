---
phase: 08-scheduled-triage-automation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/scheduled-triage/SKILL.md
autonomous: true
requirements:
  - TRIAGE-01
  - TRIAGE-02
  - TRIAGE-03
  - TRIAGE-04
  - TRIAGE-05
must_haves:
  truths:
    - "Scheduled triage skill file exists and is a complete, self-contained skill definition"
    - "Skill computes a dynamic lookback window from triage-latest.json before calling Gmail (D-03)"
    - "Skill creates Gmail drafts unconditionally for all reply-needed threads via create_draft MCP tool (D-05, D-06)"
    - "Skill reads todos/pending.md, checks for duplicates within 7 days, and appends new action items (D-07)"
    - "Skill dispatches one file per thread per matched project wiki to raw/aios/ with YAML frontmatter (D-08, D-09, D-10)"
    - "Multi-project threads dispatch to ALL matched wikis (D-09)"
    - "Skill writes triage-latest.json cache file directly and sends a PushNotification summary (D-04)"
    - "Skill preserves all critical implementation rules from daily-inbox-triage (never send, no fabricated dates, no 2RM as actionable, identity by email address not name)"
    - "Draft replies match references/voice.md register (D-05)"
  artifacts:
    - path: ".claude/skills/scheduled-triage/SKILL.md"
      provides: "Complete scheduled triage skill with Steps 0-9"
      min_lines: 200
  key_links:
    - from: ".claude/skills/scheduled-triage/SKILL.md"
      to: "aios-ui/.aios-cache/triage-latest.json"
      via: "Read for lookback window (Step 0), write after run (Step 9)"
      pattern: "triage-latest.json"
    - from: ".claude/skills/scheduled-triage/SKILL.md"
      to: "todos/pending.md"
      via: "Read for dedup check, append new items (Step 7)"
      pattern: "todos/pending.md"
    - from: ".claude/skills/scheduled-triage/SKILL.md"
      to: "{wiki}/raw/aios/triage-dispatch-*.md"
      via: "Write dispatch files per matched project (Step 8)"
      pattern: "triage-dispatch"
    - from: ".claude/skills/scheduled-triage/SKILL.md"
      to: "aios-ui/.aios-cache/triage-overrides.json"
      via: "Read override file before evaluating threads (Step 2.0)"
      pattern: "triage-overrides.json"
---

<objective>
Create the scheduled-triage skill definition (SKILL.md) that transforms email triage from a manual morning ritual into an automated routine. The skill inherits the proven thread-pulling, filtering, scoring, and context-gathering logic from daily-inbox-triage, then adds four new capabilities: dynamic lookback window computation (D-03), unconditional Gmail draft creation (D-05, D-06), persistent action item extraction with dedup (D-07, TRIAGE-03), and wiki dispatch for project-relevant intelligence (D-08, D-09, D-10, TRIAGE-04). Per D-01, this skill will be triggered by a Claude Code Desktop scheduled task (created in Plan 03).

Purpose: Justin's top pain is email reply lag. This skill makes triage ambient infrastructure -- every 2 hours during waking hours, drafts appear in Gmail, action items land in the to-do list, and project wikis accumulate email intelligence automatically.

Output: A complete, production-ready SKILL.md file at .claude/skills/scheduled-triage/SKILL.md.
</objective>

<execution_context>
@/Users/justinlobaito/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinlobaito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/justinlobaito/repos/claude-os/.planning/PROJECT.md
@/Users/justinlobaito/repos/claude-os/.planning/ROADMAP.md
@/Users/justinlobaito/repos/claude-os/.planning/STATE.md
@/Users/justinlobaito/repos/claude-os/.planning/phases/08-scheduled-triage-automation/08-RESEARCH.md
@/Users/justinlobaito/repos/claude-os/.planning/phases/08-scheduled-triage-automation/08-PATTERNS.md

# Source of truth for voice register
@/Users/justinlobaito/repos/claude-os/references/voice.md

# Existing skill to inherit from (Steps 1-5 logic)
@/Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md

# Data format targets
@/Users/justinlobaito/repos/claude-os/todos/pending.md
@/Users/justinlobaito/repos/claude-os/docs/adr/0004-staged-ingestion-via-raw-aios.md

# Contact matching source
@/Users/justinlobaito/repos/claude-os/clients.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scheduled-triage SKILL.md with Steps 0-5 (lookback + inherited triage logic)</name>
  <files>.claude/skills/scheduled-triage/SKILL.md</files>
  <read_first>
    .claude/skills/daily-inbox-triage/SKILL.md
    aios-ui/.aios-cache/triage-latest.json
    aios-ui/.aios-cache/triage-overrides.json
    references/voice.md
    clients.yaml
  </read_first>
  <action>
    Create directory .claude/skills/scheduled-triage/ and file SKILL.md within it.

    FRONTMATTER: Use the same YAML frontmatter pattern as daily-inbox-triage/SKILL.md. Set name to "scheduled-triage", description to "Automated triage routine. Fires on a Desktop scheduled task during waking hours. Pulls unanswered Gmail threads, creates draft replies, extracts action items to the to-do list, and dispatches project-relevant intelligence to wiki staging directories. This is the automated evolution of /daily-inbox-triage." Keep the three-ms-attribution block. No bike-method-phase field.

    SECTION: "What this skill does" -- One paragraph explaining this is the automated triage routine that fires on a cron schedule, creates Gmail drafts, extracts action items, and dispatches to project wikis. Reference D-01 (Desktop scheduled task), D-04 (push notification), D-05/D-06 (draft creation).

    SECTION: "Execution model" -- Explain this skill runs as a Desktop scheduled task, NOT a manual trigger. Desktop tasks have full local filesystem access but require the Desktop app to be open and machine awake. The cron expression is 0 */2 7-19 * 1-5 (every 2 hours from 7am-7pm CT, weekdays). Reference D-01, D-02. Include the explicit tradeoff note: D-01 in CONTEXT.md says "fires even when laptop is closed" but the filesystem dependencies (todos/pending.md, wiki raw/aios/ directories, triage-overrides.json) require local access. Desktop tasks are correct for this workload. Cloud Routines would require committing these files to git (future phase consideration).

    SECTION: "Today's context" -- Same pattern as daily-inbox-triage. Date placeholder {today}, inbox justin@beardedgingerdesigns.com, active engagements list. Keep the hard constraint about not faking voice.

    STEP 0 -- Compute lookback window (per D-03):
    Read /Users/justinlobaito/repos/claude-os/aios-ui/.aios-cache/triage-latest.json.
    If file missing or exitCode !== 0: lookback = "14d" (first run or last run failed), mode = "first_run".
    Else compute elapsed_hours = (now - Date.parse(ranAt)) / 3600000:
      If elapsed_hours > 48: lookback = "3d" (weekend/holiday gap), mode = "morning_briefing"
      Else if elapsed_hours > 12: lookback = "14h" (overnight gap), mode = "morning_briefing"
      Else: lookback = ceil(elapsed_hours + 1) + "h" (mid-day buffer), mode = "midday_check"

    STEP 1 -- Pull candidate threads: Copy verbatim from daily-inbox-triage Step 1. Replace the fixed "newer_than:14d" with "newer_than:{lookback}" using the computed value from Step 0.

    STEP 2 -- Filter to "Justin owes a reply": Copy verbatim from daily-inbox-triage Steps 2.0, 2.1, and 2.2. All three sub-steps identical -- override file read, thread-participant check, heuristic filter. Absolute path to triage-overrides.json: /Users/justinlobaito/repos/claude-os/aios-ui/.aios-cache/triage-overrides.json.

    STEP 3 -- Score each qualifying thread: Copy the scoring table verbatim from daily-inbox-triage Step 3. Same signals, same point values, same tiers (Reply today >= 8, Reply this week 4-7, FYI 1-3, Archive 0).

    STEP 4 -- Attach project memory context: Copy verbatim from daily-inbox-triage Step 4, including the identity rule (match on email address, never first name alone), the 4.1 dedup+fan-out pattern, the prompt template, the agent failure modes. Contact matching uses clients.yaml contacts: lists.

    STEP 5 -- Output the queue: Copy the markdown brief format verbatim from daily-inbox-triage Step 5 (ranked queue with project context). Same output structure. Also copy Step 7 (structured todos envelope with TODOS_JSON markers) verbatim -- the AIOS UI reads this envelope.
  </action>
  <verify>
    <automated>test -f /Users/justinlobaito/repos/claude-os/.claude/skills/scheduled-triage/SKILL.md && grep -c "Step 0" /Users/justinlobaito/repos/claude-os/.claude/skills/scheduled-triage/SKILL.md && grep -c "lookback" /Users/justinlobaito/repos/claude-os/.claude/skills/scheduled-triage/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .claude/skills/scheduled-triage/SKILL.md
    - Frontmatter name field is "scheduled-triage"
    - Step 0 reads triage-latest.json at absolute path /Users/justinlobaito/repos/claude-os/aios-ui/.aios-cache/triage-latest.json
    - Step 0 handles four branches: file missing (14d), elapsed > 48h (3d), elapsed > 12h (14h), else (ceil+1 hours)
    - Step 1 uses "newer_than:{lookback}" not a fixed "newer_than:14d"
    - Step 2 reads triage-overrides.json at absolute path and preserves all three sub-steps (2.0, 2.1, 2.2) verbatim
    - Step 3 scoring table matches daily-inbox-triage exactly
    - Step 4 includes the identity rule about email address matching
    - Step 5 includes both the markdown brief format and the TODOS_JSON envelope
    - grep -c "triage-overrides.json" returns at least 1
    - grep -c "clients.yaml" returns at least 1
  </acceptance_criteria>
  <done>SKILL.md exists with Steps 0-5 complete. Dynamic lookback window computed from triage-latest.json. All inherited triage logic (filter, score, context) copied verbatim from daily-inbox-triage. No behavioral drift from the proven triage pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Add Steps 6-9 to SKILL.md (draft creation, action items, dispatch, cache+notification)</name>
  <files>.claude/skills/scheduled-triage/SKILL.md</files>
  <read_first>
    .claude/skills/scheduled-triage/SKILL.md
    .claude/skills/daily-inbox-triage/SKILL.md
    todos/pending.md
    docs/adr/0004-staged-ingestion-via-raw-aios.md
    references/voice.md
  </read_first>
  <action>
    Append Steps 6-9 to the existing SKILL.md created in Task 1. Also append the output contract, critical implementation rules, and KPI tracking sections.

    STEP 6 -- Create Gmail drafts for all reply-needed threads (per D-05, D-06):
    For each thread in "Reply today" and "Reply this week" tiers, create an actual Gmail draft via mcp__claude_ai_Gmail__create_draft. Do NOT ask for confirmation -- this is the key behavioral difference from daily-inbox-triage. Daily-inbox-triage Step 6 says "ask first, never auto-create." This scheduled skill REMOVES that gate and always creates drafts.
    Draft content instructions: use references/voice.md register. Short sentences, casual greeting ("Hey {Name},"), no em dashes, plain-language status, contractions welcome. Attach project context from Step 4. The thread_id parameter links the draft to the correct Gmail conversation. Never send -- only draft. If a draft already exists on the thread from a prior run, skip creating a duplicate.

    STEP 7 -- Extract action items to todos/pending.md (per D-07, TRIAGE-03):
    For each "Reply today" and "Reply this week" thread that contains an explicit commitment, concrete request, or clear deliverable deadline:
    1. Read /Users/justinlobaito/repos/claude-os/todos/pending.md
    2. Scan the ## Pending section for items where Client: slug matches the current thread's resolved client slug AND the bold summary text overlaps significantly (same subject root, same type of request) AND Added: date is within the last 7 days. If a match is found, SKIP -- do not create a duplicate. Items older than 7 days are not considered duplicates (legitimate recurring tasks must not be silently deduped, per RESEARCH.md Pitfall 3).
    3. If no recent duplicate exists, append a new item at the bottom of the ## Pending section:

    Format (must match existing pattern exactly):
    - [ ] **{Bold action summary}** `#{category}`
      - Added: YYYY-MM-DD
      - Source: triage
      - Client: {client-slug} / {project-slug}
      - Priority: high | medium | low
      - Notes: Re: {email subject} from {sender name}

    Priority heuristics: deadline mentions within 7 days = high, routine requests or non-urgent asks = medium, soft suggestions or low-confidence = low.
    Category heuristic: #reply for reply-needed, #deliverable for concrete output requests, #meeting for scheduling, #review for review/approval requests, #follow-up for waiting-on-response items.

    STEP 8 -- Dispatch project-relevant threads to wiki raw/aios/ (per D-08, D-09, D-10, TRIAGE-04, TRIAGE-05):
    For each thread where clients.yaml contact matching (Step 4) resolved a project_slug AND the thread contains project-relevant intelligence beyond a routine status ping:

    Classification gate (TRIAGE-05): Use heuristic rules first. A thread is project-relevant if it contains: deliverable requests, deadline changes, scope changes, budget/pricing discussions, technical decisions, new requirements, launch date updates, or partner/vendor communications about the project. Routine "got it" or "thanks" acknowledgments are NOT dispatched. If unsure whether a thread is project-relevant or purely operational, err on the side of dispatching -- let the wiki Layer 2 evaluator handle redundancy.

    Write one file per thread per matched project wiki (D-08, D-09). Multi-project threads dispatch to ALL matched wikis -- each wiki gets the full thread context. Use the Write tool to create files at the correct path.

    File path: {resolved wiki path}/raw/aios/triage-dispatch-YYYY-MM-DD-{slugified-subject}.md
    Resolve wiki paths using clients.yaml docs_paths for each matched project. The slug is derived from the email subject: lowercase, replace non-alphanumeric runs with hyphens, strip leading/trailing hyphens, cap at 60 chars.

    YAML frontmatter (D-10):
    ---
    kind: triage-dispatch
    thread_id: {Gmail thread ID}
    sender: {sender email address}
    sender_name: {sender display name from email header}
    date: YYYY-MM-DD
    subject: "{email subject}"
    matched_contacts:
      - {email addresses that triggered the match}
    matched_projects:
      - {project slug}
    urgency: high | medium | low
    score: {numeric triage score from Step 3}
    ---

    Body: 3-5 sentence summary of the project-relevant content. NOT raw email text. Cover: what was asked or told, any action required, relevant dates or deadlines mentioned, context for why this matters to the project.

    STEP 9 -- Write cache and send notification (per D-04):
    Write /Users/justinlobaito/repos/claude-os/aios-ui/.aios-cache/triage-latest.json directly using the Write tool. Shape must match TriageCacheEntry exactly:
    {
      "ranAt": "{ISO timestamp of this run}",
      "output": "{full markdown output from Step 5}",
      "exitCode": 0,
      "durationMs": {elapsed milliseconds from start of Step 0 to now}
    }
    Use 2-space indented JSON (matching writeTriageCache format for UI compatibility).

    Send PushNotification:
      title: "Inbox Triage -- {N} threads need reply"
      body: "Highest priority: {top thread subject} ({client name}, {days waiting}d)"
    If 0 threads qualify: title "Inbox Triage -- clear", body "No unanswered threads as of {time}."

    OUTPUT CONTRACT section (after all steps):
    Every run produces:
    1. One Markdown brief (Step 5) -- ranked queue with project context
    2. One structured JSON envelope (Step 5) -- feeds dashboard todo cards
    3. Gmail drafts for all reply-needed threads (Step 6) -- created automatically
    4. Action items appended to todos/pending.md (Step 7) -- with dedup check
    5. Dispatch files in matched project wikis (Step 8) -- one per thread per wiki
    6. Updated triage-latest.json cache (Step 9) -- for next run's lookback
    7. Push notification summary (Step 9) -- nudge to check Gmail

    CRITICAL IMPLEMENTATION RULES section:
    Copy all 9 rules from daily-inbox-triage verbatim (never send, don't fabricate dates, don't surface 2RM as actionable, inbox clean message, no first-name conflation, render actual sender name from header). Add one new rule:
    10. Write cache LAST. If any earlier step fails, do not write triage-latest.json -- this ensures the next run re-processes the same window.

    KPI TRACKING section: Copy from daily-inbox-triage. Same bucket (less cost + more value per customer), same metric (% replied within 24 biz hours), updated baseline to note that the scheduled routine should improve this metric by removing the manual trigger bottleneck.
  </action>
  <verify>
    <automated>grep -c "Step 6\|Step 7\|Step 8\|Step 9" /Users/justinlobaito/repos/claude-os/.claude/skills/scheduled-triage/SKILL.md && grep -c "create_draft" /Users/justinlobaito/repos/claude-os/.claude/skills/scheduled-triage/SKILL.md && grep -c "todos/pending.md" /Users/justinlobaito/repos/claude-os/.claude/skills/scheduled-triage/SKILL.md && grep -c "triage-dispatch" /Users/justinlobaito/repos/claude-os/.claude/skills/scheduled-triage/SKILL.md && grep -c "PushNotification" /Users/justinlobaito/repos/claude-os/.claude/skills/scheduled-triage/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - Step 6 uses mcp__claude_ai_Gmail__create_draft and does NOT ask for confirmation
    - Step 6 references references/voice.md for draft register
    - Step 7 reads todos/pending.md at absolute path /Users/justinlobaito/repos/claude-os/todos/pending.md
    - Step 7 dedup check scans for same Client: slug AND similar summary AND Added: within 7 days
    - Step 7 append format matches the existing pending.md pattern exactly (checkbox, bold summary, hashtag, indented metadata)
    - Step 8 dispatches one file per thread per matched wiki
    - Step 8 file naming follows triage-dispatch-YYYY-MM-DD-{slug}.md convention
    - Step 8 YAML frontmatter includes all required fields: kind, thread_id, sender, sender_name, date, subject, matched_contacts, matched_projects, urgency, score
    - Step 9 writes triage-latest.json at absolute path with TriageCacheEntry shape
    - Step 9 sends PushNotification with thread count and highest priority thread
    - Critical implementation rules include "Never send" and all 9 rules from daily-inbox-triage
    - grep -c "Never send" returns at least 1
    - grep -c "voice.md" returns at least 1
    - Total SKILL.md line count exceeds 200 lines (wc -l)
  </acceptance_criteria>
  <done>SKILL.md is complete with all 10 steps (0-9). Draft creation is unconditional. Action item extraction writes to todos/pending.md with dedup. Wiki dispatch creates one file per thread per wiki. Cache write and push notification fire as final step. All critical implementation rules preserved from daily-inbox-triage. The skill is a self-contained, production-ready definition ready for scheduling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gmail thread content -> draft reply | Email body from untrusted senders is used as context for draft generation |
| Gmail thread content -> todo item | Email subject/sender used in to-do item summary and Notes field |
| Gmail thread content -> dispatch file | Email subject/sender/body used in YAML frontmatter and summary body of dispatch files |
| Gmail thread content -> push notification | Email subject used in notification body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Spoofing | Draft reply voice | mitigate | Skill references voice.md for register; drafts are never sent, only staged for human review in Gmail |
| T-08-03 | Tampering | todos/pending.md append | accept | Append-only pattern; single-operator local system; no access control needed. File is human-editable. |
| T-08-04 | Tampering | dispatch file path | mitigate | Skill constructs paths from clients.yaml docs_paths (trusted source) + slugified email subject (sanitized to [a-z0-9-]). Absolute path validation by skill before write. |
| T-08-05 | Information Disclosure | PushNotification content | accept | Notification shows email subject from Gmail -- operator's own inbox. No PII exposure beyond what the operator already has access to. |
| T-08-06 | Denial of Service | Gmail search volume | mitigate | pageSize: 50 caps thread fetch. Lookback window prevents unbounded search. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
1. `test -f /Users/justinlobaito/repos/claude-os/.claude/skills/scheduled-triage/SKILL.md` -- file exists
2. `wc -l /Users/justinlobaito/repos/claude-os/.claude/skills/scheduled-triage/SKILL.md` -- at least 200 lines
3. `grep -c "create_draft" .claude/skills/scheduled-triage/SKILL.md` -- at least 2 (Step 6 + output contract)
4. `grep -c "todos/pending.md" .claude/skills/scheduled-triage/SKILL.md` -- at least 2 (Step 7 + output contract)
5. `grep -c "triage-dispatch" .claude/skills/scheduled-triage/SKILL.md` -- at least 3 (Step 8 + frontmatter + output contract)
6. `grep -c "PushNotification" .claude/skills/scheduled-triage/SKILL.md` -- at least 1 (Step 9)
7. `grep -c "Never send" .claude/skills/scheduled-triage/SKILL.md` -- at least 1 (critical rules)
</verification>

<success_criteria>
- Scheduled triage SKILL.md is complete with Steps 0-9
- Dynamic lookback window reads from triage-latest.json (D-03)
- Gmail drafts created unconditionally for reply-needed threads (D-05, D-06)
- Action items extracted with 7-day dedup check, appended to todos/pending.md (D-07, TRIAGE-03)
- Wiki dispatch creates one file per thread per matched wiki with YAML frontmatter (D-08, D-09, D-10, TRIAGE-04)
- Heuristic classification gates dispatch; LLM only for ambiguous (TRIAGE-05)
- Cache updated and push notification sent (D-04)
- All critical implementation rules preserved from daily-inbox-triage
- SKILL.md line count exceeds 200
</success_criteria>

<output>
Create `.planning/phases/08-scheduled-triage-automation/08-02-SUMMARY.md` when done
</output>
