---
phase: 04-bidirectional-hub
plan: 04
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - aios-ui/lib/cache/triage-overrides.ts
  - aios-ui/lib/types.ts
  - aios-ui/app/api/triage/override/[threadId]/route.ts
  - aios-ui/components/triage-row-actions.tsx
  - aios-ui/components/triage-output.tsx
  - aios-ui/components/communications-section.tsx
  - aios-ui/app/clients/[client]/projects/[project]/page.tsx
  - /Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md
  - aios-ui/tests/lib/cache/triage-overrides.test.ts
  - aios-ui/tests/app/api/triage-override.test.ts
autonomous: true
requirements: [HUB-04, HUB-09]
must_haves:
  truths:
    - "POST /api/triage/override/[threadId] with body { status, snooze_until? } atomically writes .aios-cache/triage-overrides.json"
    - "Override route emits a triage_override receipt (kind=triage_override, project_slug from request body, file_written=absolute path to triage-overrides.json, excerpt='{status} for thread {threadId}')"
    - "Override route publishes an invalidationBus message scoped to the project so the UI refetches"
    - "triage-row-actions.tsx renders three buttons (Replied / Snooze 2d / Not me) per thread, posts to the override route, and uses optimistic UI"
    - "triage-output.tsx accepts an optional projectSlug + projectContacts prop; when present, filters rows to threads whose participants match any contact"
    - "Communications section on Project page renders TriageOutput scoped to that project's contacts (filtered) + row actions"
    - "Daily-inbox-triage SKILL.md is updated: Step 2 reads triage-overrides.json BEFORE evaluating 'Justin owes a reply' and skips replied/not_me/dismissed/snoozed-not-elapsed threads; Step 2 also calls mcp__claude_ai_Gmail__get_thread on borderline threads to check whether justin@beardedgingerdesigns.com appears as sender on ANY message"
  artifacts:
    - path: "aios-ui/lib/cache/triage-overrides.ts"
      provides: "Atomic read/write of triage-overrides.json"
      exports: ["readOverrides", "writeOverride", "TriageOverride", "TriageOverridesFile"]
    - path: "aios-ui/app/api/triage/override/[threadId]/route.ts"
      provides: "POST endpoint persisting per-thread overrides + emitting receipts"
    - path: "aios-ui/components/triage-row-actions.tsx"
      provides: "Per-row Replied/Snooze/Not me action buttons"
    - path: "aios-ui/components/communications-section.tsx"
      provides: "Project-page Communications surface"
    - path: "/Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md"
      provides: "Updated Step 2 that respects overrides + checks Gmail thread participants"
  key_links:
    - from: "aios-ui/app/api/triage/override/[threadId]/route.ts"
      to: "aios-ui/lib/cache/receipts.ts"
      via: "appendReceipt({ kind: 'triage_override', ... })"
      pattern: "appendReceipt"
    - from: "aios-ui/components/triage-row-actions.tsx"
      to: "/api/triage/override"
      via: "fetch POST"
      pattern: "fetch.*api/triage/override"
    - from: "/Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md"
      to: ".aios-cache/triage-overrides.json"
      via: "Read step 2"
      pattern: "triage-overrides.json"
---

<objective>
Build the per-project triage with per-row override (HUB-04, HUB-09): a state file (triage-overrides.json) the daily-inbox-triage skill reads before filtering, a POST endpoint that writes to it, per-row action buttons (Replied / Snooze 2d / Not me), and a Communications section on the Project page that filters triage rows to that project's contacts.

Purpose: Operator complaint — "daily ingest assumes I haven't replied even when I have." Override gives the operator the final word; per-project filter prevents one big inbox view.

Output: Override persistence + endpoint + skill update + UI surfaces + tests. This plan depends on 04-01 (receipt persistence + raw-drops helpers) and 04-03 (WikiDisplay wiring of the Project page — serializes the project-page edits within Wave 2 to avoid file collisions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bidirectional-hub/04-CONTEXT.md
@.planning/phases/04-bidirectional-hub/04-00-DESIGN.md
@.planning/phases/04-bidirectional-hub/04-01-SUMMARY.md
@.planning/phases/04-bidirectional-hub/04-03-SUMMARY.md
@CLAUDE.md
@aios-ui/CLAUDE.md
@aios-ui/AGENTS.md
@docs/adr/0003-bidirectional-store-model.md

# Existing patterns — read once.
@aios-ui/lib/cache/sessions.ts
@aios-ui/lib/cache/triage.ts
@aios-ui/components/triage-output.tsx
@aios-ui/components/todo-list.tsx
@aios-ui/app/api/todos/[id]/route.ts
@aios-ui/app/api/triage/run/route.ts
@aios-ui/app/api/triage/latest/route.ts
@aios-ui/app/clients/[client]/projects/[project]/page.tsx

# Next.js 16 docs — this is NOT the Next.js you know (per aios-ui/AGENTS.md). Read before writing route or component code.
@aios-ui/node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md
@aios-ui/node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md

<interfaces>
Types added to aios-ui/lib/types.ts:
- TriageOverrideStatus = 'replied' | 'snoozed' | 'not_me' | 'dismissed'
- TriageOverride { status: TriageOverrideStatus; marked_at: string; snooze_until?: string }
- TriageOverridesFile { [threadId: string]: TriageOverride }

lib/cache/triage-overrides.ts exports:
- overridesPath(): string
- readOverrides(): Promise<TriageOverridesFile>
- writeOverride(threadId: string, override: TriageOverride): Promise<void> (atomic via temp+rename)
- deleteOverride(threadId: string): Promise<void>

POST /api/triage/override/[threadId] request body:
- { status: TriageOverrideStatus; snooze_until?: string; project_slug?: string }
- snooze_until required when status === 'snoozed'

TriageOutput props (extend existing component):
- markdown: string
- projectSlug?: string
- projectContacts?: string[]   // emails or '@domain.com' patterns from clients.yaml
- renderRowActions?: boolean   // when true, render TriageRowActions on each thread row
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add triage-override types + lib/cache/triage-overrides.ts + POST route + tests</name>
  <read_first>
    - aios-ui/lib/cache/sessions.ts (atomic-write pattern — write+rename)
    - aios-ui/lib/cache/triage.ts (cache dir resolution via AIOS_CACHE_DIR)
    - aios-ui/lib/cache/receipts.ts (appendReceipt signature from 04-01)
    - aios-ui/lib/raw-drops.ts (slugify helper if needed for excerpt)
    - aios-ui/lib/invalidation-bus.ts (publish API)
    - aios-ui/app/api/todos/[id]/route.ts (POST handler pattern + invalidationBus.publish)
    - aios-ui/tests/lib/cache/sessions.test.ts (Vitest tmpdir pattern)
    - aios-ui/node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md (Next.js 16 route-handler conventions — params is a Promise, runtime/dynamic exports)
  </read_first>
  <behavior>
    - readOverrides() returns {} when file missing (no throw)
    - writeOverride(id, ov) creates the cache dir, writes to a .tmp file, renames to overrides.json (atomic)
    - writeOverride twice for the same id overwrites the entry
    - deleteOverride removes the key; does not error if key absent
    - POST /api/triage/override/abc123 with body { status: 'replied', project_slug: 'wild-rose' } returns 200 with { ok: true }, writes to file, appends one line to receipts.jsonl with kind=triage_override + project_slug=wild-rose + excerpt='replied for thread abc123', and publishes one invalidationBus message
    - POST with status='snoozed' but no snooze_until returns 400 with error 'snooze_until required when status=snoozed'
    - POST with invalid status returns 400
    - POST with non-JSON body returns 400
  </behavior>
  <files>
    - aios-ui/lib/types.ts (extend with TriageOverrideStatus, TriageOverride, TriageOverridesFile)
    - aios-ui/lib/cache/triage-overrides.ts (NEW)
    - aios-ui/app/api/triage/override/[threadId]/route.ts (NEW)
    - aios-ui/tests/lib/cache/triage-overrides.test.ts (NEW)
    - aios-ui/tests/app/api/triage-override.test.ts (NEW)
  </files>
  <action>
    1. Extend aios-ui/lib/types.ts: append the three types listed in the <interfaces> block above.

    2. Create aios-ui/lib/cache/triage-overrides.ts:
       - overridesPath(): returns path.join(AIOS_CACHE_DIR env or process.cwd()+'.aios-cache', 'triage-overrides.json'). Mirror cacheDir() from sessions.ts.
       - readOverrides(): try fs.readFile + JSON.parse; on ENOENT, return {}.
       - writeOverride(threadId, override): read current file → set file[threadId] = override → mkdir cache dir recursive → write to overridesPath()+'.tmp' → fs.rename to overridesPath() (atomic).
       - deleteOverride(threadId): same pattern; delete file[threadId] before write.

    3. Create aios-ui/app/api/triage/override/[threadId]/route.ts:
       - export const dynamic = 'force-dynamic'
       - export const runtime = 'nodejs'
       - VALID statuses array: ['replied', 'snoozed', 'not_me', 'dismissed']
       - POST handler:
         a) parse params.threadId
         b) parse JSON body (catch → 400 'invalid JSON body')
         c) validate status in VALID (else 400)
         d) if status==='snoozed' validate snooze_until present (else 400)
         e) build TriageOverride { status, marked_at: new Date().toISOString(), snooze_until if present }
         f) await writeOverride(threadId, override)
         g) await appendReceipt({ id: 'rcpt_'+random, ts: ISO, kind: 'triage_override', project_slug: body.project_slug ?? '', file_written: overridesPath(), excerpt: status+' for thread '+threadId, actor: 'triage-row-actions' })
         h) invalidationBus.publish({ scope: { kind: 'global' }, reason: 'triage override '+threadId+' '+status, at: ISO })
         i) return Response.json({ ok: true })

    4. Tests in aios-ui/tests/lib/cache/triage-overrides.test.ts:
       - Use AIOS_CACHE_DIR=os.tmpdir()+uuid override per test; clean in afterEach.
       - readOverrides on missing file returns {}
       - writeOverride creates file with one entry; readOverrides returns it
       - writeOverride twice with same id overwrites
       - deleteOverride removes a key; does not error if missing
       - After writeOverride, no .tmp file remains (atomic rename succeeded)

    5. Tests in aios-ui/tests/app/api/triage-override.test.ts:
       - Import the POST handler directly and invoke with mock Request.
       - Happy path: status='replied' returns 200; file has entry; receipts.jsonl has one line with kind=triage_override.
       - Error: status='snoozed' without snooze_until returns 400.
       - Error: status='bogus' returns 400.
       - Error: non-JSON body returns 400.

    6. Run: cd aios-ui && npm test -- triage-override. All pass.
  </action>
  <verify>
    <automated>cd aios-ui && npm test -- triage-override 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export async function writeOverride" aios-ui/lib/cache/triage-overrides.ts returns 1
    - grep -c "fs.rename" aios-ui/lib/cache/triage-overrides.ts >= 1 (atomic write)
    - grep -c "export async function POST" aios-ui/app/api/triage/override/\[threadId\]/route.ts returns 1
    - grep -c "appendReceipt" aios-ui/app/api/triage/override/\[threadId\]/route.ts returns 1
    - grep -c "invalidationBus.publish" aios-ui/app/api/triage/override/\[threadId\]/route.ts returns 1
    - grep -c "triage_override" aios-ui/app/api/triage/override/\[threadId\]/route.ts >= 1
    - cd aios-ui && npm test -- triage-override 2>&1 | grep -E "passed|failed" shows tests PASS, 0 failed
  </acceptance_criteria>
  <done>Override persistence + endpoint + receipt emission + tests committed.</done>
</task>

<task type="auto">
  <name>Task 2: Update daily-inbox-triage SKILL.md to respect overrides + Gmail thread-participant check</name>
  <read_first>
    - /Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md (the project-local AIOS skill — read the entire existing file; preserve all current Step 1, Step 3, and the TODOS_JSON envelope structure)
    - aios-ui/lib/skills/daily-ingest.ts (the TODOS_JSON envelope contract must remain intact)
  </read_first>
  <action>
    1. Edit the existing file at /Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md (the project-local AIOS skill — confirmed exists; do NOT create at any other location). Make the changes below as IN-PLACE edits to the existing file; preserve frontmatter and all other steps unchanged.

    2. ADD a new sub-step 2.0 at the START of "Step 2: Triage threads where Justin owes a reply":

       Sub-step content (literal text to insert):
       - Heading: "2.0 — Read the override file BEFORE evaluating any thread."
       - Instruction: Read /Users/justinlobaito/repos/claude-os/aios-ui/.aios-cache/triage-overrides.json. If the file does not exist, treat as {}. Keyed by Gmail thread ID; each value has status (replied | snoozed | not_me | dismissed) and optionally snooze_until (ISO).
       - Rules:
         * If overrides[threadId].status === 'replied' → SKIP. Do not surface.
         * If overrides[threadId].status === 'not_me' → SKIP. Do not surface.
         * If overrides[threadId].status === 'dismissed' → SKIP. Do not surface.
         * If overrides[threadId].status === 'snoozed' AND Date.parse(snooze_until) > Date.now() → SKIP. (Expired snoozes fall through and are evaluated normally.)

    3. ADD a new sub-step 2.1 immediately after 2.0:

       - Heading: "2.1 — Gmail thread-participant check for borderline cases."
       - Instruction: The existing 'last sender ≠ justin@beardedgingerdesigns.com AND >18h since last activity' heuristic misclassifies threads where Justin has replied mid-thread. For each thread that passes the heuristic, additionally call mcp__claude_ai_Gmail__get_thread(thread_id) and inspect messages[*].headers.From. If justin@beardedgingerdesigns.com appears as the sender on ANY message in the thread, downgrade or drop — Justin is already in the loop.

    4. Renumber the rest of Step 2's sub-steps if they previously started at 2.1 — push them to 2.2+.

    5. Do NOT alter Step 1, Step 3, or the TODOS_JSON_START / TODOS_JSON_END envelope contract — the UI's extractTodosEnvelope (aios-ui/lib/skills/daily-ingest.ts) depends on it.

    6. Smoke-verify: head -120 the file to confirm Step 2.0 + 2.1 are present and the structure is intact.
  </action>
  <verify>
    <automated>grep -c "triage-overrides.json" /Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md ; grep -c "mcp__claude_ai_Gmail__get_thread" /Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md ; grep -c "snooze_until" /Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md ; grep -c "TODOS_JSON_START" /Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - File /Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md continues to exist (was not deleted/moved)
    - grep -c "triage-overrides.json" /Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md returns >= 1
    - grep -c "mcp__claude_ai_Gmail__get_thread" /Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md returns >= 1
    - grep -c "snooze_until" /Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md returns >= 1
    - grep -c "TODOS_JSON_START" /Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md returns >= 1 (envelope preserved)
    - File still has the existing Step 1 + Step 3 structure (grep "Step 1" + "Step 3" each returns >= 1)
  </acceptance_criteria>
  <done>SKILL.md updated in place at the project-local path; override-aware Step 2 in place; envelope contract preserved.</done>
</task>

<task type="auto">
  <name>Task 3: TriageRowActions component + TriageOutput projectSlug/projectContacts filter + CommunicationsSection</name>
  <read_first>
    - aios-ui/components/triage-output.tsx (current implementation — need to extend with optional props)
    - aios-ui/components/todo-list.tsx (optimistic-update pattern with fetch POST)
    - aios-ui/components/ui/button.tsx (shadcn Button)
    - aios-ui/lib/data/clients.ts (getProject signature; how to read contacts)
    - aios-ui/app/clients/[client]/projects/[project]/page.tsx (current Project page structure for insertion point; 04-03 has just wired WikiDisplay above the Source-files section)
    - aios-ui/lib/cache/triage.ts (readTriageCache; how to get the markdown to render)
    - aios-ui/node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md (Next.js 16 server vs. client component boundaries — verify async server components + 'use client' directive semantics)
  </read_first>
  <action>
    1. Create aios-ui/components/triage-row-actions.tsx as a CLIENT component:
       - 'use client' directive at top.
       - Props: { threadId: string; projectSlug: string }
       - State: status: 'idle' | 'pending' | 'replied' | 'snoozed' | 'not_me' | 'error'; errorMsg?: string
       - Three buttons (small, in a row): "Replied", "Snooze 2d", "Not me"
       - On click, set status='pending' (optimistic), then POST /api/triage/override/{threadId} with body { status: <chosen>, snooze_until: chosen==='snoozed' ? ISO of now+2d : undefined, project_slug: projectSlug }
       - On success: set status to the chosen action; render confirmation text ("Snoozed for 2 days" / "Marked replied" / "Marked not me"). No undo control in this iteration — CONTEXT.md does not require it; operator can re-run any action via a fresh row after the next /daily-inbox-triage. If a future iteration adds undo, it would require a DELETE/clear endpoint.
       - On error: set status='error', show errorMsg inline.

    2. Modify aios-ui/components/triage-output.tsx to accept the new optional props:
       - Add interface: { markdown: string; projectSlug?: string; projectContacts?: string[]; renderRowActions?: boolean }
       - When projectSlug + projectContacts provided:
         a) parse the markdown into thread blocks. Heuristic: a "thread block" is a paragraph beginning with a heading like '###' or a 'Thread:' line; collect every line up to the next thread-block boundary.
         b) for each block, capture the thread ID via THREAD_ID_REGEX (already exists).
         c) keep the block only if at least one of projectContacts appears anywhere in the block body (case-insensitive substring match). A contact like '@example.com' matches any occurrence of 'example.com' in the body.
         d) re-join the kept blocks into the rendered markdown.
       - When renderRowActions=true: after each Gmail-link line, insert a <TriageRowActions threadId={id} projectSlug={projectSlug!} /> block.
       - Keep the existing behavior when no projectSlug is provided (zero changes from current functionality).

    3. Create aios-ui/components/communications-section.tsx as a SERVER component:
       - Props: { clientSlug: string; projectSlug: string; projectContacts: string[] }
       - Async: read readTriageCache() (existing helper in lib/cache/triage.ts) → if no cache, render empty-state "No daily triage cached. Run /daily-inbox-triage from the Home page first."
       - If cache exists: render <h2>Communications</h2> + <TriageOutput markdown={cache.output} projectSlug={projectSlug} projectContacts={projectContacts} renderRowActions />
       - Show a small "Last triage: <relative time>" footer using cache.ranAt.

    4. Wire CommunicationsSection into the Project page (aios-ui/app/clients/[client]/projects/[project]/page.tsx):
       - This plan depends on 04-03 — the WikiDisplay block from 04-03 is already wired above the Source-files section. Do NOT touch the WikiDisplay block or the hasNonWikiSourceFiles guard added by 04-03.
       - Add import: import { CommunicationsSection } from '@/components/communications-section'
       - Insertion point (explicit anchor): place AFTER the existing Source-files / hasNonWikiSourceFiles block AND BEFORE the CaptureBox section. If a future plan (04-09) needs to insert Pending Ingestion in the same region, it must place itself AFTER CommunicationsSection per its own plan; this plan only inserts the single CommunicationsSection node.
       - Render: <CommunicationsSection clientSlug={client.slug} projectSlug={project.slug} projectContacts={allContacts} />
       - 'contacts' is the existing array already filtered to exclude '@domain' patterns. CommunicationsSection needs BOTH plain emails and @domain patterns for filtering, so pass the original raw list: const allContacts = project.contacts ?? []. Use allContacts (not the filtered contacts).

    5. Smoke verify build: cd aios-ui && npm run build. Clean.
  </action>
  <verify>
    <automated>cd aios-ui && npx tsc --noEmit 2>&1 | head -20 ; cd aios-ui && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "'use client'" aios-ui/components/triage-row-actions.tsx returns 1
    - grep -c "fetch.*api/triage/override" aios-ui/components/triage-row-actions.tsx >= 1
    - grep -c "Replied" aios-ui/components/triage-row-actions.tsx >= 1
    - grep -c "Snooze" aios-ui/components/triage-row-actions.tsx >= 1
    - grep -c "Not me" aios-ui/components/triage-row-actions.tsx >= 1
    - grep -c "projectSlug" aios-ui/components/triage-output.tsx >= 1
    - grep -c "projectContacts" aios-ui/components/triage-output.tsx >= 1
    - grep -c "CommunicationsSection" aios-ui/app/clients/\[client\]/projects/\[project\]/page.tsx returns 2 (import + use)
    - grep -c "WikiDisplay" aios-ui/app/clients/\[client\]/projects/\[project\]/page.tsx >= 2 (the 04-03 wiring must still be present — verifies no regression)
    - cd aios-ui && npm run build 2>&1 | grep -Ec "Failed to compile|error TS" returns 0
  </acceptance_criteria>
  <done>Per-row override buttons + per-project filter + CommunicationsSection rendered on Project page; build passes; 04-03 WikiDisplay wiring preserved.</done>
</task>

</tasks>

<verification>
1. cd aios-ui && npm test — all tests pass.
2. cd aios-ui && npm run build — clean build.
3. Manual smoke: Run npm run dev. Open a Project page (e.g., /clients/wild-rose-casino/projects/wild-rose). Verify:
   - The Communications section appears between the Source-files block and the Capture box.
   - The WikiDisplay block from 04-03 is still visible above the Source-files block (no regression).
   - If a daily triage cache exists, rows are filtered to threads mentioning the project's contacts (e.g., only meghan@... rows for Wild Rose).
   - Each row shows three small action buttons.
   - Clicking Replied immediately marks the row optimistically; .aios-cache/triage-overrides.json contains the entry; receipt dock shows a triage_override row.
   - Re-running /daily-inbox-triage from Home would (in real life) skip overridden threads — verify the override file is read at the path the skill expects: /Users/justinlobaito/repos/claude-os/aios-ui/.aios-cache/triage-overrides.json.
</verification>

<success_criteria>
- triage-overrides.json correctly persists per-thread overrides
- POST /api/triage/override emits one receipt per call (kind=triage_override)
- daily-inbox-triage SKILL.md at /Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md respects overrides + checks Gmail thread participants on borderline threads
- Project page Communications section filters triage to project contacts and renders row actions
- WikiDisplay wiring from 04-03 still in place
- All tests pass (existing + new for cache helper + endpoint)
</success_criteria>

<output>
After completion, create .planning/phases/04-bidirectional-hub/04-04-SUMMARY.md documenting:
- Confirmation that the edits were applied in place to /Users/justinlobaito/repos/claude-os/.claude/skills/daily-inbox-triage/SKILL.md
- The thread-block parsing heuristic used for the projectContacts filter (in case it needs refinement later)
- Any deviation from "atomic write" (e.g., if EXDEV happened on rename, fall back to write+unlink+rename)
</output>
