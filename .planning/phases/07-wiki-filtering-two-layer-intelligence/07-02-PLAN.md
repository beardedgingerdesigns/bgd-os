---
phase: 07-wiki-filtering-two-layer-intelligence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - aios-ui/lib/types.ts
  - aios-ui/lib/skills/wiki-ingest.ts
  - aios-ui/tests/lib/skills/wiki-ingest.test.ts
  - aios-ui/tests/fixtures/fake-claude-wiki-ingest.sh
  - .claude/skills/ingest-aios-drops/SKILL.md
autonomous: true
requirements:
  - WIKI-02
  - WIKI-03
must_haves:
  truths:
    - "Wiki ingest evaluates each raw/aios/ drop against existing wiki knowledge before promoting"
    - "Ingest produces three distinct outcomes per file: promote (new info), skip (redundant), flag (contradicts)"
    - "Skipped files include a logged reason explaining why the content was redundant"
    - "Flagged files include contradiction detail: incoming claim, existing claim, existing page path, severity"
    - "The structured summary envelope carries promoted, skipped, and contested arrays"
    - "The contested array contains objects (not strings) with file and contradiction detail"
  artifacts:
    - path: "aios-ui/lib/types.ts"
      provides: "Updated WikiIngestSummary with rich contested objects and new skipped array"
      contains: "ContestedEntry"
    - path: "aios-ui/lib/skills/wiki-ingest.ts"
      provides: "Updated parser handling rich contested/skipped envelope"
      exports: ["WikiIngestSummary", "ContestedEntry", "SkippedEntry"]
    - path: ".claude/skills/ingest-aios-drops/SKILL.md"
      provides: "Enhanced skill with evaluation step producing promote/skip/flag verdicts"
      contains: "Evaluation pass"
  key_links:
    - from: "aios-ui/lib/skills/wiki-ingest.ts"
      to: "aios-ui/lib/types.ts"
      via: "import ContestedEntry, SkippedEntry types"
      pattern: "ContestedEntry"
    - from: ".claude/skills/ingest-aios-drops/SKILL.md"
      to: "aios-ui/lib/skills/wiki-ingest.ts"
      via: "Skill emits JSON envelope that wiki-ingest.ts parses"
      pattern: "INGEST_SUMMARY"
---

<objective>
Enhance the wiki ingest pipeline with evaluation intelligence (WIKI-02, WIKI-03). The /ingest-aios-drops skill gains an evaluation step that compares each incoming raw/aios/ drop against existing wiki knowledge and produces one of three verdicts: promote, skip, or flag. The WikiIngestSummary type and parser are updated to carry rich structured data for each verdict.

Purpose: Without evaluation, every raw drop is blindly promoted into the wiki, creating redundancy and silently overwriting contradictory information. This is Layer 2 of the two-layer intelligence pipeline.

Output: Enhanced SKILL.md with evaluation logic, updated TypeScript types and parser, extended test fixtures and tests.
</objective>

<execution_context>
@/Users/justinlobaito/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinlobaito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/justinlobaito/repos/claude-os/.planning/PROJECT.md
@/Users/justinlobaito/repos/claude-os/.planning/ROADMAP.md
@/Users/justinlobaito/repos/claude-os/.planning/STATE.md
@/Users/justinlobaito/repos/claude-os/.planning/phases/07-wiki-filtering-two-layer-intelligence/07-RESEARCH.md

# Codebase context
@/Users/justinlobaito/repos/claude-os/aios-ui/lib/skills/wiki-ingest.ts
@/Users/justinlobaito/repos/claude-os/aios-ui/lib/types.ts
@/Users/justinlobaito/repos/claude-os/aios-ui/tests/lib/skills/wiki-ingest.test.ts
@/Users/justinlobaito/repos/claude-os/aios-ui/tests/fixtures/fake-claude-wiki-ingest.sh
@/Users/justinlobaito/repos/claude-os/.claude/skills/ingest-aios-drops/SKILL.md
@/Users/justinlobaito/repos/claude-os/aios-ui/app/api/wiki/ingest/[client]/[project]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update types, parser, and skill definition for three-outcome evaluation</name>
  <files>aios-ui/lib/types.ts, aios-ui/lib/skills/wiki-ingest.ts, .claude/skills/ingest-aios-drops/SKILL.md</files>
  <read_first>
    aios-ui/lib/types.ts
    aios-ui/lib/skills/wiki-ingest.ts
    .claude/skills/ingest-aios-drops/SKILL.md
    aios-ui/app/api/wiki/ingest/[client]/[project]/route.ts
  </read_first>
  <action>
    Three files need coordinated updates to establish the enhanced ingest contract.

    TYPES (aios-ui/lib/types.ts): This file does NOT currently define WikiIngestSummary (that type lives in wiki-ingest.ts). Leave types.ts alone for now -- the new types go in wiki-ingest.ts where they already live. (If the executor finds WikiIngestSummary was moved to types.ts since the plan was written, add the new types there instead.)

    WIKI-INGEST.TS (aios-ui/lib/skills/wiki-ingest.ts): Update the WikiIngestSummary interface and add two new exported interfaces:

    Export interface ContestedEntry with fields: file (string), contradiction object containing incoming_claim (string), existing_claim (string), existing_page (string), severity ("high" | "medium" | "low").

    Export interface SkippedEntry with fields: file (string), reason (string).

    Update WikiIngestSummary: promoted stays as string[]. Change deferred from string[] to string[] (keep as-is -- deferred means "error, could not process"). Add skipped as SkippedEntry[]. Change contested from string[] to ContestedEntry[].

    Update the summary parser in the child.on("close") handler (around line 176-189). The current parser coerces contested to string[]. Update it to: (a) parse contested as ContestedEntry[] -- if each element is an object with file and contradiction fields, keep it; if it is a plain string (backward compat with old skill), wrap it as { file: element, contradiction: { incoming_claim: "unknown", existing_claim: "unknown", existing_page: "unknown", severity: "medium" } }. (b) parse skipped as SkippedEntry[] -- if each element is an object with file and reason, keep it; if plain string, wrap as { file: element, reason: "unknown" }. (c) keep promoted and deferred as string[] (no change). This backward-compatible parsing ensures existing ingest runs that emit the old string[] format do not break.

    SKILL.MD (.claude/skills/ingest-aios-drops/SKILL.md): Replace Step 3 ("Process each pending file") with the enhanced evaluation workflow. Keep Step 1 (resolve pending drops) and Step 2 (bootstrap structure) unchanged. The new Step 3 content:

    "### Step 3: Evaluate and process each pending file

    For each pending file (in mtime ascending order -- oldest first):

    1. Read the file contents (frontmatter + body).
    2. **Evaluation pass:**
       a. Identify the topic/entity the file describes from its content.
       b. Read existing wiki pages that cover the same topic. Search index.md for matching terms. Check decisions/active/ for related slugs. Read sources/ entries for overlapping dates or topics.
       c. Compare the incoming content against existing wiki knowledge:
          - If the file contains information NOT in any existing wiki page: verdict is **PROMOTE**.
          - If the file's content is substantially covered by existing wiki pages (same facts, same dates, same decisions): verdict is **SKIP** with logged reason.
          - If the file's content CONTRADICTS an existing wiki page (different date for same event, reversed decision, conflicting status at the same point in time): verdict is **FLAG**.
       d. IMPORTANT: A status update is NOT a contradiction. A superseded decision is NOT a contradiction. A contradiction is when two sources claim different facts about the same event or entity at the same point in time. Be specific about the contradiction.
    3. For PROMOTE files: invoke the llm-wiki ingest flow as before (unchanged from current Step 3.2).
    4. For SKIP files: do NOT promote. Record the reason.
    5. For FLAG files: do NOT promote. Record the contradiction detail (incoming claim, existing claim, existing page, severity)."

    Update Step 4 (structured summary) to emit the new envelope shape:
    ```json
    {
      "promoted": ["filename1.md"],
      "deferred": ["filename-with-error.md"],
      "skipped": [
        { "file": "filename2.md", "reason": "Content fully covered by existing wiki/overview.md" }
      ],
      "contested": [
        {
          "file": "filename3.md",
          "contradiction": {
            "incoming_claim": "Launch date moved to July 15",
            "existing_claim": "Launch date is June 16",
            "existing_page": "wiki/pages/timeline.md",
            "severity": "high"
          }
        }
      ]
    }
    ```

    Add explicit notes to Step 4: "skipped = intentionally not promoted because content is redundant with existing wiki knowledge. deferred = could not be processed due to an error; retry on next ingest run. contested = contradicts existing knowledge; operator must resolve before promotion."
  </action>
  <verify>
    <automated>cd /Users/justinlobaito/repos/claude-os/aios-ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - WikiIngestSummary in wiki-ingest.ts has four fields: promoted (string[]), deferred (string[]), skipped (SkippedEntry[]), contested (ContestedEntry[])
    - ContestedEntry exported from wiki-ingest.ts with file, contradiction (incoming_claim, existing_claim, existing_page, severity) fields
    - SkippedEntry exported from wiki-ingest.ts with file, reason fields
    - Parser handles both new object format and legacy string format for backward compatibility
    - SKILL.md Step 3 contains "Evaluation pass" with promote/skip/flag logic
    - SKILL.md Step 4 shows the new JSON envelope with skipped and rich contested
    - tsc --noEmit produces no NEW errors (pre-existing route-type errors are acceptable per STATE.md blockers)
  </acceptance_criteria>
  <done>WikiIngestSummary type expanded with ContestedEntry and SkippedEntry. Parser handles both legacy string[] and new object[] formats. SKILL.md enhanced with evaluation logic producing three verdicts per file.</done>
</task>

<task type="auto">
  <name>Task 2: Extend test fixtures and tests for enhanced ingest envelope</name>
  <files>aios-ui/tests/lib/skills/wiki-ingest.test.ts, aios-ui/tests/fixtures/fake-claude-wiki-ingest.sh</files>
  <read_first>
    aios-ui/lib/skills/wiki-ingest.ts
    aios-ui/tests/lib/skills/wiki-ingest.test.ts
    aios-ui/tests/fixtures/fake-claude-wiki-ingest.sh
  </read_first>
  <action>
    Update the test fixture and add new test cases to verify the enhanced summary parsing.

    FIXTURE (aios-ui/tests/fixtures/fake-claude-wiki-ingest.sh): The existing fixture emits a summary with promoted (2 files), deferred (1 file), and contested (empty array). Add a new mode triggered by a --rich-summary flag (follow the existing --no-summary and --fail flag patterns). When --rich-summary is passed, emit a summary envelope with: promoted: ["capture-2026-06-04-thermal-kitchen-launch.md"], deferred: [], skipped: [{ "file": "capture-2026-06-04-status-check.md", "reason": "Content fully covered by existing wiki/overview.md" }], contested: [{ "file": "chat-decision-2026-06-04-launch-shifted.md", "contradiction": { "incoming_claim": "Launch date moved to July 15", "existing_claim": "Launch date is June 16", "existing_page": "wiki/pages/timeline.md", "severity": "high" } }]. Emit the same stream-json wrapper format as the existing fixture. Keep all existing fixture modes working unchanged.

    Also add a --legacy-contested mode that emits contested as plain strings (the old format): contested: ["some-file.md"]. This tests backward compatibility.

    TESTS (aios-ui/tests/lib/skills/wiki-ingest.test.ts): Add three new test cases following the existing pattern (create tmp wrapper scripts that call the fixture with specific flags):

    Test "rich summary: parses ContestedEntry and SkippedEntry objects": Use --rich-summary fixture. Assert result.summary.promoted has length 1. Assert result.summary.skipped has length 1 and skipped[0].file is "capture-2026-06-04-status-check.md" and skipped[0].reason contains "fully covered". Assert result.summary.contested has length 1 and contested[0].file is "chat-decision-2026-06-04-launch-shifted.md" and contested[0].contradiction.incoming_claim is "Launch date moved to July 15" and contested[0].contradiction.severity is "high".

    Test "legacy contested strings: wraps as ContestedEntry with unknown fields": Use --legacy-contested fixture. Assert result.summary.contested has length 1 and contested[0].file is "some-file.md" and contested[0].contradiction.incoming_claim is "unknown".

    Test "summary with empty skipped: skipped defaults to empty array": Use the existing default fixture (no flags). Assert result.summary.skipped is defined and has length 0 (backward compat -- old envelope has no skipped field, parser defaults to []).
  </action>
  <verify>
    <automated>cd /Users/justinlobaito/repos/claude-os/aios-ui && npx vitest run tests/lib/skills/wiki-ingest.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - fake-claude-wiki-ingest.sh supports --rich-summary and --legacy-contested flags without breaking existing modes
    - wiki-ingest.test.ts has 3 new test cases: rich summary parsing, legacy backward compat, empty skipped default
    - All existing wiki-ingest tests still pass
    - New tests verify ContestedEntry object structure (file, contradiction.incoming_claim, contradiction.severity)
    - New tests verify SkippedEntry object structure (file, reason)
    - npx vitest run tests/lib/skills/wiki-ingest.test.ts -x passes all tests
  </acceptance_criteria>
  <done>Test fixture supports rich and legacy summary formats. Three new test cases verify ContestedEntry/SkippedEntry parsing and backward compatibility. All wiki-ingest tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill subprocess -> parser | JSON envelope emitted by claude subprocess is parsed; malformed JSON handled gracefully |
| LLM evaluation -> verdict | The skill's LLM evaluation of wiki contradictions could produce false positives or miss real conflicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | INGEST_SUMMARY_RE JSON parse | mitigate | Existing try/catch around JSON.parse; malformed envelope gracefully degrades to summary: undefined. Extended to handle both object and string array shapes. |
| T-07-05 | Repudiation | skip verdict | accept | Skip reason is logged in the summary envelope and displayed in UI. Operator can re-run ingest to re-evaluate. Files in raw/aios/ are immutable (ADR 0004). |
| T-07-06 | Information Disclosure | contradiction detail | accept | Contradiction detail contains wiki content excerpts shown only to the local operator. Single-user, local-only system. |
</threat_model>

<verification>
1. `cd /Users/justinlobaito/repos/claude-os/aios-ui && npx vitest run tests/lib/skills/wiki-ingest.test.ts -x` — all wiki-ingest tests pass (existing + new)
2. `cd /Users/justinlobaito/repos/claude-os/aios-ui && npx vitest run -x` — full suite green
3. `grep -c "ContestedEntry" aios-ui/lib/skills/wiki-ingest.ts` — returns 1+
4. `grep -c "Evaluation pass" .claude/skills/ingest-aios-drops/SKILL.md` — returns 1+
</verification>

<success_criteria>
- WikiIngestSummary carries promoted, deferred, skipped, and contested arrays with rich types
- Parser backward-compatible with old string[] contested format
- SKILL.md contains evaluation logic producing promote/skip/flag verdicts per file
- 6+ wiki-ingest tests pass (3 existing + 3 new)
- Full test suite passes
</success_criteria>

<output>
Create `.planning/phases/07-wiki-filtering-two-layer-intelligence/07-02-SUMMARY.md` when done
</output>
