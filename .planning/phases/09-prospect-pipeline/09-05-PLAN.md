---
phase: 09-prospect-pipeline
plan: 05
type: execute
wave: 2
depends_on: [1]
files_modified:
  - .claude/skills/weekly-project-status/SKILL.md
  - ~/.claude/skills/ask-the-board/SKILL.md
autonomous: true
requirements:
  - PROSPECT-01
must_haves:
  truths:
    - "weekly-project-status includes a Prospect Pipeline section when active/warm prospects exist"
    - "Prospect Pipeline section is omitted when no active/warm prospects exist (no clutter per D-17)"
    - "Each prospect in the pipeline section shows: slug, display name, status, last_activity, contact count, next steps"
    - "/ask-the-board resolves prospect doc paths when a prospect slug is mentioned"
    - "Board advisors receive the full prospect dossier for pricing, positioning, and strategy advice per D-06"
  artifacts:
    - path: ".claude/skills/weekly-project-status/SKILL.md"
      provides: "Extended weekly status with conditional Prospect Pipeline section"
    - path: "~/.claude/skills/ask-the-board/SKILL.md"
      provides: "Extended board skill with prospect doc path resolution (user-level skill)"
  key_links:
    - from: ".claude/skills/weekly-project-status/SKILL.md"
      to: "prospects/<slug>.md"
      via: "Reads all prospect docs to build pipeline summary"
      pattern: "prospects/"
    - from: "~/.claude/skills/ask-the-board/SKILL.md"
      to: "prospects/<slug>.md"
      via: "Resolves prospect slug to doc path for board advisor context"
      pattern: "prospects/"
---

<objective>
Extend weekly project status to surface the prospect pipeline and extend /ask-the-board to resolve prospect doc paths. The weekly status gains a conditional "Prospect Pipeline" section that appears only when active or warm prospects exist (D-17). The board skill gains the ability to reference prospect dossiers when a prospect slug is mentioned (D-06), giving advisors full context for pricing and positioning advice.

These are the visibility integrations that make the prospect pipeline a first-class part of the AIOS operating rhythm.
</objective>

<execution_context>
@/Users/justinlobaito/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinlobaito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/justinlobaito/repos/claude-os/.planning/PROJECT.md
@/Users/justinlobaito/repos/claude-os/.planning/ROADMAP.md
@/Users/justinlobaito/repos/claude-os/.planning/STATE.md
@/Users/justinlobaito/repos/claude-os/.planning/phases/09-prospect-pipeline/09-CONTEXT.md
@/Users/justinlobaito/repos/claude-os/.claude/skills/weekly-project-status/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Prospect Pipeline section to weekly-project-status</name>
  <files>.claude/skills/weekly-project-status/SKILL.md</files>
  <read_first>
    .claude/skills/weekly-project-status/SKILL.md
    .planning/phases/09-prospect-pipeline/09-CONTEXT.md
  </read_first>
  <action>
    Read weekly-project-status SKILL.md fully. The skill currently enumerates active projects from clients.yaml and fans out to project-researcher agents.

    Add a new step after the project enumeration that scans for active prospects:

    Step N -- Prospect Pipeline Scan:
    1. List all files in `prospects/` directory (excluding `converted/` subdirectory)
    2. For each prospect doc:
       a. Read YAML frontmatter
       b. If `status` is `active` or `warm`: include in pipeline
       c. Extract: slug (from filename), display name (from H1), status, last_activity, contact count (from contacts map), next steps (from Next Steps section -- first 2 bullet points)
    3. If zero active/warm prospects: skip the Prospect Pipeline section entirely (D-17)
    4. If one or more active/warm prospects: add a "Prospect Pipeline" section to the weekly status output

    Section format:
    ```markdown
    ## Prospect Pipeline

    | Prospect | Status | Last Activity | Contacts | Next Steps |
    |----------|--------|---------------|----------|------------|
    | <display_name> | <status> | <last_activity> | <count> | <first next step> |

    Cold/Dead: <count> (not shown above)
    ```

    Position this section after the active projects section but before the summary/recommendations section. Prospects are not projects -- they're pipeline, and the visual separation matters.

    Also: check the `status_changed_at` field in each prospect's frontmatter. If `status_changed_at` is within the past 7 days AND `previous_status` was a higher-activity status (active→cold, warm→dead, etc.), add a cooling alert:
    ```
    ⚠ Recently cooled: <slug> went <status> on <status_changed_at> (was <previous_status>)
    ```
    This uses the `status_changed_at` and `previous_status` fields written by the auto-flip logic in Plan 02's triage extension.
  </action>
  <verify>
    <automated>grep -c "Prospect Pipeline\|prospects/\|active.*warm" /Users/justinlobaito/repos/claude-os/.claude/skills/weekly-project-status/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains a Prospect Pipeline scan step
    - Section only appears when active/warm prospects exist
    - Pipeline table includes: prospect name, status, last_activity, contact count, next steps
    - Cold/dead count shown as summary line below table
    - Recently cooled prospects flagged with warning
    - grep -c "Prospect Pipeline" in SKILL.md returns at least 1
    - grep -c "prospects/" in SKILL.md returns at least 2
  </acceptance_criteria>
  <done>Weekly project status includes conditional Prospect Pipeline section with pipeline table and cooling alerts.</done>
</task>

<task type="auto">
  <name>Task 2: Add prospect doc resolution to /ask-the-board</name>
  <files>~/.claude/skills/ask-the-board/SKILL.md</files>
  <read_first>
    ~/.claude/skills/ask-the-board/SKILL.md
    .planning/phases/09-prospect-pipeline/09-CONTEXT.md
  </read_first>
  <action>
    Read ask-the-board SKILL.md (user-level skill at ~/.claude/skills/ask-the-board/SKILL.md, NOT in the repo). The skill currently gathers context files for advisor agents to read.

    Add prospect doc path resolution per D-06: when the user's question mentions a slug that matches a file in `prospects/`, include that prospect doc in the advisor's context files.

    Detection logic:
    1. Extract candidate slugs from the user's question (words that look like kebab-case identifiers)
    2. For each candidate, check if `prospects/<candidate>.md` exists
    3. If found, add the prospect doc path to the `<files_to_read>` block passed to each advisor agent
    4. Also check `prospects/converted/<candidate>.md` for recently converted prospects

    This gives advisors the full dossier -- timeline, problem map, pricing strategy, contacts -- so they can give informed advice on pricing, positioning, and deal strategy.

    No changes to the advisor agent prompts needed. The prospect doc is structured markdown that advisors can read like any other context file.
  </action>
  <verify>
    <automated>grep -c "prospects/\|prospect.*doc\|prospect.*path" /Users/justinlobaito/.claude/skills/ask-the-board/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains prospect doc path resolution logic
    - Slug detection scans user's question for kebab-case identifiers
    - Both `prospects/<slug>.md` and `prospects/converted/<slug>.md` checked
    - Matching prospect docs added to advisor agent files_to_read
    - grep -c "prospects/" in SKILL.md returns at least 2
  </acceptance_criteria>
  <done>Board advisors receive prospect dossiers when prospect slugs are mentioned in questions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Prospect docs read-only during weekly status and board queries. No mutation risk.
- Advisor agents receive prospect docs as context -- same trust model as existing project context files.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Information Disclosure | Prospect data in board context | accept | Advisors are local Claude agents, not external services. Prospect data stays on-device. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
1. `grep -c "Prospect Pipeline" .claude/skills/weekly-project-status/SKILL.md` -- at least 1
2. `grep -c "prospects/" .claude/skills/weekly-project-status/SKILL.md` -- at least 2
3. `grep -c "prospects/" ~/.claude/skills/ask-the-board/SKILL.md` -- at least 2
</verification>

<success_criteria>
- Weekly project status shows Prospect Pipeline section when active/warm prospects exist
- Pipeline section omitted when no active/warm prospects (clean output)
- /ask-the-board resolves prospect doc paths for advisor context
- Board advisors get full dossier for pricing and positioning advice
</success_criteria>

<output>
Create `.planning/phases/09-prospect-pipeline/09-05-SUMMARY.md` when done
</output>
