---
phase: 06-persistent-to-do-list-skill-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/onboard-client/SKILL.md
autonomous: true
requirements: [SKILL-02]

must_haves:
  truths:
    - "/onboard-client chains to /kickoff-project after completing its core registry writes"
    - "/onboard-client writes an initial STATE.md to both the project wiki root and state/<slug>.md"
    - "/onboard-client logs a full-lifecycle decisions log entry documenting the complete chain"
    - "The chain is graceful: if /kickoff-project fails, the core onboard writes (clients.yaml, memory, decisions log) are already committed"
    - "The initial STATE.md matches the format from scripts/state-hook/state-prompt.md"
  artifacts:
    - path: ".claude/skills/onboard-client/SKILL.md"
      provides: "Rewired onboard-client skill with full lifecycle chain"
      contains: "kickoff-project"
  key_links:
    - from: ".claude/skills/onboard-client/SKILL.md"
      to: "~/.claude/skills/kickoff-project/SKILL.md"
      via: "Skill chaining instruction in Phase 6 (Kickoff)"
      pattern: "kickoff-project"
    - from: ".claude/skills/onboard-client/SKILL.md"
      to: "scripts/state-hook/state-prompt.md"
      via: "STATE.md format reference for initial seed"
      pattern: "state-prompt"
    - from: ".claude/skills/onboard-client/SKILL.md"
      to: "state/<slug>.md"
      via: "Copy of initial STATE.md synced to claude-os"
      pattern: "state/"
---

<objective>
Rewire /onboard-client to trigger the full intake-to-kickoff lifecycle in a single uninterrupted flow (SKILL-02).

Purpose: Currently /onboard-client ends at registry writes (clients.yaml, memory, reference doc, decisions log). The rewired version chains through /kickoff-project to seed the wiki, writes an initial STATE.md to both the wiki and state/<slug>.md, and logs the complete lifecycle. This takes a project from "new contact" to "ready for first working session" in one command.

Output: Updated .claude/skills/onboard-client/SKILL.md with three new phases (Kickoff, Initial STATE.md, Final Close) appended after the existing Phase 5 (Write).
</objective>

<execution_context>
@/Users/justinlobaito/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinlobaito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/justinlobaito/repos/claude-os/.planning/ROADMAP.md
@/Users/justinlobaito/repos/claude-os/.planning/STATE.md
@/Users/justinlobaito/repos/claude-os/.planning/phases/06-persistent-to-do-list-skill-lifecycle/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire /onboard-client with full lifecycle chain</name>
  <files>.claude/skills/onboard-client/SKILL.md</files>
  <read_first>
    @/Users/justinlobaito/repos/claude-os/.claude/skills/onboard-client/SKILL.md (full file -- understand Phases 1-6 structure, frontmatter, output contract, critical implementation rules)
    @/Users/justinlobaito/.claude/skills/kickoff-project/SKILL.md (understand what /kickoff-project does: 11 phases, requires a project slug, creates docs/wiki/ with WIKI-CLAUDE.md, writes project-root CLAUDE.md, syncs clients.yaml docs_paths)
    @/Users/justinlobaito/repos/claude-os/scripts/state-hook/state-prompt.md (STATE.md format: H1 "Project State: {name}", metadata line "Updated: YYYY-MM-DD | Status: On track", then H2 sections: Accomplishments, Current Status, Next Steps, Blockers, Key Dates)
    @/Users/justinlobaito/repos/claude-os/.planning/phases/06-persistent-to-do-list-skill-lifecycle/06-RESEARCH.md (Pattern 3: Skill Chaining, Example 1: Initial STATE.md, Example 2: Decisions Log Entry, Anti-Pattern about chain breaks)
  </read_first>
  <action>
Edit .claude/skills/onboard-client/SKILL.md to add three new phases after the existing Phase 5 (Write) and modify the existing Phase 6 (Close) into a Phase 8 (Final Close). The existing Phases 1-5 remain UNCHANGED. The existing Phase 6 body is replaced with an expanded version.

Changes to the frontmatter:
- Update the description to mention the full lifecycle: append " Full lifecycle: intake interview, clients.yaml entry, /kickoff-project wiki seed, initial STATE.md, decisions log entry." to the end of the existing description value.

New Phase 6 (Kickoff) -- insert after the existing Phase 5 (Write) section:
- Title: "Phase 6 -- Kickoff (chained)"
- Before invoking kickoff, print a confirmation prompt: "Core onboard writes are complete (clients.yaml, memory, reference doc, decisions log). Ready to run /kickoff-project {project-slug} to seed the wiki. Continue, or skip for now?"
- If operator says continue: instruct Claude to "Now run /kickoff-project {project-slug}" -- Claude follows the kickoff-project skill instructions (11 phases: resolve, gather, interview, seed wiki, write CLAUDE.md, gut-check, dump raw docs, ingest, sync clients.yaml, git commit, report)
- If operator says skip: note in the closing summary that kickoff was deferred and the operator should run /kickoff-project {project-slug} manually when ready
- CRITICAL graceful failure handling: if /kickoff-project errors (repo creation fails, user cancels interview, filesystem error), catch the failure and continue to Phase 7 and 8. The core onboard writes from Phase 5 are already on disk. Print: "Kickoff encountered an issue: {error context}. Core onboard is complete. Run /kickoff-project {project-slug} manually when ready."

New Phase 7 (Initial STATE.md) -- runs after Phase 6 completes (or after Phase 6 skip/failure):
- Title: "Phase 7 -- Initial STATE.md"
- Only runs if Phase 6 (Kickoff) succeeded (wiki was seeded). If kickoff was skipped or failed, skip this phase with a note.
- Write an initial STATE.md to the project wiki root at ~/repos/{project-slug}/docs/wiki/STATE.md using the EXACT format from scripts/state-hook/state-prompt.md:
  - H1: "# Project State: {project-name}"
  - Metadata: "**Updated:** {YYYY-MM-DD} | **Status:** On track"
  - H2 "## Current Status": "Newly onboarded via /onboard-client. Wiki seeded by /kickoff-project. Ready for first working session."
  - H2 "## Next Steps": three task-list items: run first working session in the project repo, verify wiki seed accuracy, capture first project decision in the wiki
  - H2 "## Key Dates": one entry with today's date and "Onboarded into AIOS"
  - Omit "## Accomplishments" and "## Blockers" sections (per the state-prompt.md rule: "Omit any section that has no content")
- Copy the STATE.md to state/{project-slug}.md in the claude-os repo root
- Both writes should be atomic (write temp file, rename)

Modify existing Phase 6 (Close) into Phase 8 (Final Close):
- Rename to "Phase 8 -- Final Close"
- Expand the "What was created" checklist to include:
  - "wiki seeded at ~/repos/{project-slug}/docs/wiki/ via /kickoff-project" (or "skipped -- run manually" if deferred)
  - "STATE.md written to wiki root + state/{project-slug}.md" (or "skipped -- kickoff was deferred" if skipped)
- Update the decisions/log.md entry format (the one written in Phase 5, step 5) to include kickoff and STATE.md status. Add these lines to the Details block:
  - "Wiki: seeded at ~/repos/{project-slug}/docs/wiki/" (or "not seeded -- kickoff deferred")
  - "STATE.md: initial version written to wiki root + state/{project-slug}.md" (or "not written -- kickoff deferred")
- Update the "Verify it works" section: instead of "Run /load {project-slug}", say "Open ~/repos/{project-slug}/ in a new terminal. Claude reads CLAUDE.md + wiki automatically."
- Update the "What's next" section: remove the "/load {project-slug}" reference, replace with "Open the project repo and start your first working session. The wiki has everything."

Update the Output contract section:
- Add item 7: "One initial STATE.md in the project wiki root (if kickoff completed)"
- Add item 8: "One synced STATE.md copy at state/{project-slug}.md (if kickoff completed)"
- Amend item 5 (decisions/log.md entry) to mention the full lifecycle completion

Update Critical implementation rules:
- Add rule 10: "Graceful chain degradation. Phase 5 (Write) completes ALL core registry writes before Phase 6 (Kickoff) begins. If kickoff fails or is skipped, the operator has a fully registered client/project. Never roll back Phase 5 writes because of a Phase 6 failure."
- Add rule 11: "STATE.md format parity. The initial STATE.md written in Phase 7 MUST match the format from scripts/state-hook/state-prompt.md exactly so the Phase 5 SessionEnd hook can update it seamlessly in future sessions."

Do NOT change:
- Phases 1-4 (Client identity, Project identity, Sources, Preview + confirm) -- these remain exactly as-is
- Phase 5 (Write) steps 1-4 (clients.yaml, memory file, reference doc, MEMORY.md index) -- these remain exactly as-is
- Phase 5 step 5 (decisions/log.md entry) -- keep the existing entry format but expand the template to include wiki and STATE.md status lines
- The KPI section, Bike Method phasing section, or First-run notes section
  </action>
  <verify>
    <automated>grep -q "kickoff-project" /Users/justinlobaito/repos/claude-os/.claude/skills/onboard-client/SKILL.md && grep -q "Phase 7" /Users/justinlobaito/repos/claude-os/.claude/skills/onboard-client/SKILL.md && grep -q "Phase 8" /Users/justinlobaito/repos/claude-os/.claude/skills/onboard-client/SKILL.md && grep -q "STATE.md" /Users/justinlobaito/repos/claude-os/.claude/skills/onboard-client/SKILL.md && grep -q "state-prompt" /Users/justinlobaito/repos/claude-os/.claude/skills/onboard-client/SKILL.md && grep -q "Full lifecycle" /Users/justinlobaito/repos/claude-os/.claude/skills/onboard-client/SKILL.md && grep -q "Graceful chain" /Users/justinlobaito/repos/claude-os/.claude/skills/onboard-client/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter description mentions full lifecycle and /kickoff-project
    - Phases 1-5 (excluding the decisions log template expansion) are unchanged from the original
    - Phase 6 (Kickoff) includes a confirmation prompt before chaining
    - Phase 6 has graceful failure handling that does not roll back Phase 5 writes
    - Phase 7 (Initial STATE.md) writes STATE.md in the exact format from scripts/state-hook/state-prompt.md
    - Phase 7 copies STATE.md to state/{project-slug}.md
    - Phase 7 is skipped if kickoff was skipped or failed
    - Phase 8 (Final Close) shows expanded "What was created" checklist with wiki and STATE.md status
    - Output contract lists 8 artifacts (original 6 + STATE.md wiki + STATE.md sync)
    - Critical implementation rules include graceful chain degradation (rule 10) and STATE.md format parity (rule 11)
    - The "Verify it works" section no longer references /load-project
  </acceptance_criteria>
  <done>/onboard-client SKILL.md rewired with Phases 6-8 (Kickoff chain, Initial STATE.md, Final Close); running /onboard-client walks the full intake-to-kickoff lifecycle in one flow with graceful degradation if kickoff fails</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. The skill is a markdown instruction file. Execution happens within a Claude Code session operating on the local filesystem with the operator's own permissions. No network calls, no authentication, no untrusted input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | /onboard-client chain | accept | Skill chaining relies on Claude following sequential instructions. If Claude skips a step, the graceful degradation rule (rule 10) ensures core writes are already on disk. Operator can re-run deferred steps manually. |
| T-06-04 | Denial of Service | /kickoff-project subprocess | accept | Kickoff creates directories, seeds files, and may spawn a sub-agent. All local operations. Failure modes are filesystem errors or user cancellation, both handled by graceful chain degradation. |
</threat_model>

<verification>
- .claude/skills/onboard-client/SKILL.md contains "kickoff-project" chain reference
- SKILL.md contains Phase 6 (Kickoff), Phase 7 (Initial STATE.md), Phase 8 (Final Close)
- SKILL.md references scripts/state-hook/state-prompt.md for STATE.md format
- SKILL.md includes graceful failure handling for the kickoff chain
- SKILL.md output contract lists 8 artifacts
- No references to /load-project remain in the "Verify it works" or "What's next" sections
</verification>

<success_criteria>
- SKILL-02: /onboard-client triggers the full lifecycle: intake interview (Phases 1-3) -> clients.yaml + memory + reference doc + decisions log (Phase 5) -> /kickoff-project wiki seed (Phase 6) -> initial STATE.md in wiki + state/ (Phase 7) -> decisions log updated with full lifecycle (Phase 8)
- Chain is graceful: core onboard writes complete before kickoff starts; kickoff failure does not lose registry data
- Initial STATE.md matches the hook-generated format for downstream compatibility
</success_criteria>

<output>
Create .planning/phases/06-persistent-to-do-list-skill-lifecycle/06-02-SUMMARY.md when done
</output>
