---
phase: 06-persistent-to-do-list-skill-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - todos/pending.md
  - todos/completed.md
  - .claude/skills/load-project/SKILL.md
autonomous: true
requirements: [TODO-01, TODO-02, TODO-03, TODO-04, SKILL-01]

must_haves:
  truths:
    - "todos/pending.md exists in the claude-os repo root and persists across git commits"
    - "todos/completed.md exists as an archive target for finished items"
    - "The to-do format supports both manual and automated (triage) writes with parseable metadata"
    - "Email threads are excluded from the to-do list by format design (only extracted action items belong)"
    - "/load-project prints a deprecation notice directing the operator to work directly in the project repo"
    - "/load-project's trigger phrases are preserved in the SKILL.md frontmatter so Claude still recognizes the command"
  artifacts:
    - path: "todos/pending.md"
      provides: "Persistent to-do list with structured markdown format"
      contains: "## Pending"
    - path: "todos/completed.md"
      provides: "Archive for completed to-do items"
      contains: "## Completed"
    - path: ".claude/skills/load-project/SKILL.md"
      provides: "Deprecation notice replacing the original skill execution body"
      contains: "DEPRECATED"
  key_links:
    - from: "todos/pending.md"
      to: "Phase 8 triage skill (future)"
      via: "Structured format with Source: triage metadata line"
      pattern: "Source: triage"
    - from: ".claude/skills/load-project/SKILL.md"
      to: "Claude Code skill recognition"
      via: "Preserved trigger phrases in frontmatter description field"
      pattern: "description:.*load.*project"
---

<objective>
Create the persistent to-do list as a first-class AIOS artifact and retire the /load-project skill.

Purpose: The to-do list gives Justin a persistent scratch pad for action items that survives across Claude Code sessions and will serve as the write target for Phase 8 triage automation. Retiring /load-project completes the shift to self-sufficient project wikis (shipped in Phase 5).

Output: todos/pending.md (structured format), todos/completed.md (archive), updated load-project SKILL.md (deprecation notice)
</objective>

<execution_context>
@/Users/justinlobaito/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinlobaito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/justinlobaito/repos/claude-os/.planning/ROADMAP.md
@/Users/justinlobaito/repos/claude-os/.planning/STATE.md
@/Users/justinlobaito/repos/claude-os/.planning/phases/06-persistent-to-do-list-skill-lifecycle/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create persistent to-do list artifact (todos/ directory)</name>
  <files>todos/pending.md, todos/completed.md</files>
  <read_first>
    @/Users/justinlobaito/repos/claude-os/.planning/phases/06-persistent-to-do-list-skill-lifecycle/06-RESEARCH.md (Pattern 1: Structured Markdown To-Do Format section)
    @/Users/justinlobaito/repos/claude-os/aios-ui/lib/types.ts (lines 131-157 for existing Todo type definitions -- reference only, do not modify)
  </read_first>
  <action>
Create the todos/ directory at the claude-os repo root.

Create todos/pending.md with the following structure:
- H1 heading: "To-Do List"
- A brief preamble explaining that items persist until explicitly completed, and completed items are moved to completed.md
- H2 section "## Pending" as the append target for all new items
- Two seed items that reflect real open work Justin has (from STATE.md and memory):
  1. A manual item about reviewing BrandOS dealer migration status (Source: manual, Priority: medium)
  2. A manual item about verifying Phase 5 STATE.md hook fires correctly on next session end (Source: manual, Priority: high)

Each item follows the structured format from RESEARCH.md Pattern 1:
- Starts with "- [ ]" (pending checkbox)
- Bold summary on the first line, optional hashtag category tag
- Indented metadata lines: Added (today's date YYYY-MM-DD), Source (one of: manual, triage, onboard, skill:name), Client (client-slug / project-slug compound form, only if applicable), Priority (high, medium, low), Notes (optional, one-line context)

Format rules to encode in the preamble:
- Source field values: manual, triage, onboard, skill:name
- Priority field values: high, medium, low
- Client field uses client-slug / project-slug compound form from clients.yaml
- Items appended at the bottom of the Pending section
- Email threads are NEVER stored here (TODO-04) -- only extracted action items from triage belong; raw email threads remain in aios-ui/.aios-cache/todos-today.json

Create todos/completed.md with:
- H1 heading: "Completed Items"
- A brief preamble explaining this is the archive for items moved from pending.md when marked [x]
- H2 section "## Completed" (empty, ready to receive items)
  </action>
  <verify>
    <automated>test -f /Users/justinlobaito/repos/claude-os/todos/pending.md && test -f /Users/justinlobaito/repos/claude-os/todos/completed.md && grep -q "## Pending" /Users/justinlobaito/repos/claude-os/todos/pending.md && grep -q "## Completed" /Users/justinlobaito/repos/claude-os/todos/completed.md && grep -c "\- \[ \]" /Users/justinlobaito/repos/claude-os/todos/pending.md | grep -q "2" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - todos/pending.md exists with H1 heading, format rules preamble, and "## Pending" section
    - Two seed items exist with correct structured format (checkbox, bold summary, metadata lines)
    - Preamble explicitly states email threads do not belong here (TODO-04 enforcement)
    - todos/completed.md exists with H1 heading and empty "## Completed" section
    - Both files are valid markdown
  </acceptance_criteria>
  <done>todos/pending.md and todos/completed.md exist with correct structure, seed items, and format documentation</done>
</task>

<task type="auto">
  <name>Task 2: Retire /load-project skill with deprecation notice</name>
  <files>.claude/skills/load-project/SKILL.md</files>
  <read_first>
    @/Users/justinlobaito/repos/claude-os/.claude/skills/load-project/SKILL.md (current skill -- read the full frontmatter description field to preserve ALL trigger phrases)
    @/Users/justinlobaito/repos/claude-os/.planning/phases/06-persistent-to-do-list-skill-lifecycle/06-RESEARCH.md (Pattern 2: Skill Deprecation via Redirect section, and Pitfall 3)
  </read_first>
  <action>
Replace the entire contents of .claude/skills/load-project/SKILL.md with a deprecation notice.

CRITICAL: The frontmatter description field MUST preserve ALL existing trigger phrases so Claude Code still recognizes when Justin says "/load {project}", "load the {project} project", "switch to {project}", "give me everything on {project}", "hydrate {project} context", or any variant of "I'm working on {X} now, bring me up to speed". Prefix the description with "DEPRECATED -- " to signal the retirement.

Frontmatter fields to set:
- name: load-project
- description: "DEPRECATED -- previously used when Justin says /load {project}, load the {project} project, switch to {project}, give me everything on {project}, hydrate {project} context, or any variant of I'm working on {X} now bring me up to speed. Now prints a deprecation notice directing the operator to the project wiki."
- bike-method-phase: retired
- three-ms-attribution: preserve the existing attribution block verbatim

Body (below the frontmatter separator) should contain:
- H2: "This skill is deprecated"
- Explanation that /load-project was retired as of 2026-06-04 because project wikis are now self-sufficient
- "What to do instead" section with 3 numbered steps: (1) open the project repo directly via cd ~/repos/project-slug/, (2) Claude Code reads the project's CLAUDE.md and docs/wiki/WIKI-CLAUDE.md automatically, (3) the wiki contains all decisions, state, and context that /load-project used to hydrate
- "Why this changed" paragraph: the llm-wiki pattern makes each project's wiki the canonical knowledge home, and the Phase 5 SessionEnd hook auto-generates STATE.md -- loading context into the AIOS session is no longer necessary
- Suggestion to use /weekly-project-status for cross-project status instead
- Preserved Three Ms attribution footer
  </action>
  <verify>
    <automated>grep -q "DEPRECATED" /Users/justinlobaito/repos/claude-os/.claude/skills/load-project/SKILL.md && grep -q "load.*project" /Users/justinlobaito/repos/claude-os/.claude/skills/load-project/SKILL.md && grep -q "switch to" /Users/justinlobaito/repos/claude-os/.claude/skills/load-project/SKILL.md && grep -q "hydrate.*context" /Users/justinlobaito/repos/claude-os/.claude/skills/load-project/SKILL.md && grep -q "bike-method-phase: retired" /Users/justinlobaito/repos/claude-os/.claude/skills/load-project/SKILL.md && grep -q "deprecated" /Users/justinlobaito/repos/claude-os/.claude/skills/load-project/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md frontmatter description contains "DEPRECATED" prefix
    - ALL original trigger phrases preserved in the description field: "/load {project}", "load the {project} project", "switch to {project}", "give me everything on {project}", "hydrate {project} context", "I'm working on {X} now bring me up to speed"
    - bike-method-phase set to "retired"
    - three-ms-attribution block preserved
    - Body contains deprecation explanation with "What to do instead" steps
    - Body references the llm-wiki pattern and Phase 5 STATE.md hook as the reason
    - No execution instructions remain -- the entire skill body is informational
  </acceptance_criteria>
  <done>/load-project SKILL.md replaced with deprecation notice; all trigger phrases preserved in frontmatter; invoking "/load brandos" in a Claude Code session produces the deprecation message instead of the old hydration flow</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. All operations are local filesystem reads/writes within the operator's own repo. No network calls, no user input handling beyond Claude Code skill interaction, no authentication.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | todos/pending.md | accept | File is in a local git repo with full history. Operator is the only writer. Tampering risk is negligible for a single-operator local system. |
| T-06-02 | Information Disclosure | todos/pending.md | accept | File may contain client names and action items. Acceptable because the repo is local-only (ADR 0001 SS1). No cloud deploy, no multi-user. |
</threat_model>

<verification>
- todos/pending.md exists with 2 seed items in the structured format
- todos/completed.md exists with empty Completed section
- .claude/skills/load-project/SKILL.md contains deprecation notice with all trigger phrases preserved
- Running "grep -c 'DEPRECATED' .claude/skills/load-project/SKILL.md" returns at least 1
- Running "grep -c '\- \[ \]' todos/pending.md" returns 2
</verification>

<success_criteria>
- TODO-01: todos/pending.md exists as a first-class AIOS artifact committed to git
- TODO-02: Format supports automated append with Source: triage metadata (ready for Phase 8)
- TODO-03: Format is human-editable with clear instructions in the preamble
- TODO-04: Preamble explicitly excludes email threads; only extracted action items belong
- SKILL-01: /load-project shows deprecation notice; trigger phrases preserved for recognition
</success_criteria>

<output>
Create .planning/phases/06-persistent-to-do-list-skill-lifecycle/06-01-SUMMARY.md when done
</output>
